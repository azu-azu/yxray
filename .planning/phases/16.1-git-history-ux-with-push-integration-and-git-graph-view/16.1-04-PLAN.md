---
phase: 16.1-git-history-ux-with-push-integration-and-git-graph-view
plan: 04
type: execute
wave: 4
depends_on:
  - 16.1-03
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Cloud icon appears on pushed entries and is hidden for local-only entries"
    - "Cloud icons are hidden entirely when no remote is connected"
    - "Push N versions button appears when there are local-only commits and disappears after push"
    - "Graph view renders commits as nodes; clicking a node opens the diff viewer"
    - "Toggle between list and graph persists after page reload"
    - "Pushing from HistoryPanel updates cloud icons without leaving the panel"
    - "After a push from RemotePanel, HistoryPanel cloud icons update automatically"
    - "Back from DiffViewer returns to the view that was active (list or graph)"
  artifacts:
    - path: "app/frontend/src/components/HistoryPanel.tsx"
      provides: "Complete Phase 16.1 frontend implementation"
      contains: "GraphView"
    - path: "app/services/git_ops.py"
      provides: "git_pushed_shas function"
      contains: "git_pushed_shas"
    - path: "app/routers/history.py"
      provides: "Extended history response with is_pushed"
      contains: "is_pushed"
---

<objective>
Human verification checkpoint for all Phase 16.1 features: cloud icons, push button, graph view, toggle persistence, cross-panel refresh, and back-navigation.

Purpose: Confirm all visual and interactive features work correctly end-to-end before marking Phase 16.1 complete.

Output: Human approval or issue report.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-git-history-ux-with-push-integration-and-git-graph-view/16.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start app</name>
  <files></files>
  <action>
    Run the full test suite to confirm the backend is healthy:
    `cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/ -x -q`

    Then start the app:
    `cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m app.main`
    (or the project's normal start command)

    Confirm app is accessible at http://localhost:7433 (or the active port).
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All backend tests pass. App starts successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all Phase 16.1 features end-to-end</name>
  <action>Human opens the app and performs the 6 verification tests listed below.</action>
  <verify>
    <automated>MISSING — all Phase 16.1 frontend features require visual/interactive verification</automated>
  </verify>
  <done>Human types "approved" after all 6 tests pass.</done>
  <what-built>
    Phase 16.1 complete feature set:
    1. Cloud icons on pushed history entries (hidden when no remote)
    2. "↑ Back up N versions" button in HistoryPanel header (hidden when all pushed or no remote)
    3. SVG graph view with ≡ List / ▦ Graph toggle
    4. Toggle persistence via localStorage (survives page reload)
    5. Graph node click opens diff viewer
    6. Push from HistoryPanel updates cloud icons and button without leaving the panel
    7. Push from RemotePanel auto-refreshes HistoryPanel cloud icons
    8. Back from diff viewer returns to whichever view was active
  </what-built>
  <how-to-verify>
    Open the app at http://localhost:7433.

    **Setup for testing (if not already present):**
    - Add a project folder that has at least 2-3 git commits (saved versions)
    - Optionally connect GitHub or GitLab from RemotePanel and do a push to have some pushed and some local-only commits

    **Test 1 — List view cloud icons:**
    - Open HistoryPanel (a project with saved versions)
    - If connected to a remote with pushed commits: verify cloud icons appear on pushed entries
    - Verify entries that are NOT pushed show no cloud icon
    - Disconnect the remote (if applicable): verify ALL cloud icons disappear

    **Test 2 — Push button:**
    - With local-only commits (ahead > 0) and remote connected: verify "↑ Back up N versions" button appears
    - Click the button: verify it says "Backing up..." then disappears after success
    - Verify previously local-only entries now show cloud icons
    - With no remote connected: verify push button is hidden

    **Test 3 — Graph view:**
    - Click "▦ Graph" toggle: verify view switches to SVG node graph
    - Verify nodes are connected by a vertical line
    - Verify pushed nodes appear in blue (with white center dot) vs gray for local-only
    - Click a graph node: verify diff viewer opens
    - Click "← History" back button: verify returns to graph view (not list view)
    - Click "≡ List" toggle: verify view switches back to list

    **Test 4 — Toggle persistence:**
    - Switch to "▦ Graph" view
    - Reload the page (Cmd+R)
    - Verify the app opens in graph view (not list)
    - Switch back to "≡ List", reload: verify stays in list view

    **Test 5 — Cross-panel refresh:**
    - Open HistoryPanel with at least one local-only commit
    - Navigate to RemotePanel (click the remote/cloud icon in sidebar)
    - Push from RemotePanel
    - Navigate back to HistoryPanel
    - Verify cloud icons have updated on the previously local-only entries

    **Test 6 — Back navigation from graph:**
    - In graph view, click a commit node to open the diff viewer
    - Verify the back button shows "← History"
    - Click back: verify returns to graph view (not list view)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval of all Phase 16.1 features listed above.
</verification>

<success_criteria>
- All 6 manual verification tests pass
- No regressions in existing HistoryPanel functionality (list view, file selectors, undo button)
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-git-history-ux-with-push-integration-and-git-graph-view/16.1-04-SUMMARY.md`
</output>
