---
phase: 18.1-creation-of-pr
plan: 01
subsystem: api
tags: [github, gitlab, httpx, tdd, pull-request, merge-request]

# Dependency graph
requires:
  - phase: 16-remote-auth-and-push
    provides: github_api.py and gitlab_api.py with httpx patterns and _github_headers helper
provides:
  - parse_github_owner_repo, create_pull_request, get_open_pr_for_branch in github_api.py
  - parse_gitlab_namespace_path, get_gitlab_project_id, create_merge_request, get_open_mr_for_branch in gitlab_api.py
  - tests/test_pr.py with 9 passing unit tests for all new service functions
affects: [18.1-02-router, 18.1-03-ui]

# Tech tracking
tech-stack:
  added: []
  patterns: [httpx mock pattern via patch("app.services.*.httpx"), re.match URL parsing with optional .git suffix]

key-files:
  created:
    - tests/test_pr.py
  modified:
    - app/services/github_api.py
    - app/services/gitlab_api.py

key-decisions:
  - "parse_github_owner_repo uses regex r'https://github\\.com/([^/]+)/([^/]+?)(?:\\.git)?$' — handles both .git and non-.git URLs"
  - "get_gitlab_project_id encodes namespace slash as %2F manually (not urllib.parse.quote) — direct string replacement per RESEARCH.md spec"
  - "re import added to gitlab_api.py (already present in github_api.py from earlier phases)"

patterns-established:
  - "URL parse helpers raise ValueError on no match — callers can catch and surface to user"
  - "get_open_*_for_branch returns first item or None — router layer handles None as 'no existing PR/MR' signal"

requirements-completed: [PR-API]

# Metrics
duration: 2min
completed: 2026-03-21
---

# Phase 18.1 Plan 01: PR/MR Service Functions Summary

**GitHub and GitLab service functions for PR/MR creation and existence checks via httpx, driven by TDD (9 tests RED then GREEN)**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-21T23:41:31Z
- **Completed:** 2026-03-21T23:43:30Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `parse_github_owner_repo`, `create_pull_request`, `get_open_pr_for_branch` to `github_api.py`
- Added `parse_gitlab_namespace_path`, `get_gitlab_project_id`, `create_merge_request`, `get_open_mr_for_branch` to `gitlab_api.py`
- Created `tests/test_pr.py` with 9 unit tests (mocked httpx); all 9 pass; full suite 230 passed (3 pre-existing port_probe failures unchanged)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write RED tests** - `be9de4c` (test)
2. **Task 2: Implement service functions** - `a31bb65` (feat)

_Note: TDD tasks have two commits (test RED → feat GREEN)_

## Files Created/Modified

- `tests/test_pr.py` - 9 unit tests for all new service functions using `patch("app.services.*.httpx")`
- `app/services/github_api.py` - Added 3 PR helper functions after existing code
- `app/services/gitlab_api.py` - Added `import re` and 4 MR helper functions after existing code

## Decisions Made

- `parse_github_owner_repo` regex uses `([^/]+?)` with non-greedy match before optional `(?:\.git)?$` to strip suffix without consuming it
- `get_gitlab_project_id` encodes `/` as `%2F` via direct string replace per RESEARCH.md — no need for `urllib.parse.quote`
- `re` import added to `gitlab_api.py`; `github_api.py` already had it from Phase 11 slugify function

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All 7 service functions are implemented and tested; Plan 02 (router) can call them directly
- `parse_github_owner_repo` and `parse_gitlab_namespace_path` provide URL normalization the router needs for storing remote URLs from config

---
*Phase: 18.1-creation-of-pr*
*Completed: 2026-03-21*

## Self-Check: PASSED

- tests/test_pr.py: FOUND
- app/services/github_api.py: FOUND
- app/services/gitlab_api.py: FOUND
- 18.1-01-SUMMARY.md: FOUND
- commit be9de4c: FOUND
- commit a31bb65: FOUND
