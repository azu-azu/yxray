---
phase: 18.1-creation-of-pr
plan: 04
type: execute
wave: 4
depends_on:
  - 18.1-03
files_modified: []
autonomous: false
requirements:
  - PR-API
  - PR-ENDPOINT
  - PR-UI
must_haves:
  truths:
    - "On an experiment branch, the Remote panel shows an 'Open pull request' button in the GitHub tab"
    - "Clicking the button expands an inline form with a pre-filled humanized title derived from the branch name"
    - "Submitting the form creates a real GitHub PR (or simulated) and shows a 'View on GitHub' link inline"
    - "On a fresh panel load with an existing open PR for the branch, 'View pull request' link is shown instead of the create button"
    - "The PR button is absent when on main branch"
    - "The GitLab tab shows 'Open merge request' with identical flow"
    - "Full pytest suite passes"
    - "Frontend builds with zero TS errors"
  artifacts:
    - path: "app/services/github_api.py"
      provides: "create_pull_request, get_open_pr_for_branch, parse_github_owner_repo"
    - path: "app/services/gitlab_api.py"
      provides: "create_merge_request, get_open_mr_for_branch, get_gitlab_project_id, parse_gitlab_namespace_path"
    - path: "app/routers/remote.py"
      provides: "POST /api/remote/pr/create, GET /api/remote/pr/status"
    - path: "app/frontend/src/components/RemotePanel.tsx"
      provides: "Inline PR creation form, existing PR link, branch humanization"
  key_links:
    - from: "RemotePanel.tsx (browser)"
      to: "POST /api/remote/pr/create"
      via: "handleCreatePR fetch"
      pattern: "fetch.*pr/create"
    - from: "RemotePanel.tsx (browser)"
      to: "GET /api/remote/pr/status"
      via: "fetchPRStatus on mount"
      pattern: "fetch.*pr/status"
---

<objective>
Human verification checkpoint — confirm the complete PR creation flow works end-to-end in the running app.

Purpose: Validate all Phase 18.1 requirements from the user's perspective before marking the phase complete.
Output: Human approval that all behaviors work correctly, or a list of issues to fix.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-creation-of-pr/18.1-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete PR/MR creation flow in the Remote panel:
    - Backend: create_pull_request (GitHub), create_merge_request (GitLab), parse helpers, and two new endpoints
    - Frontend: inline PR form in Remote panel GitHub + GitLab tabs, branch name humanization, existing-PR check on mount, success state with link
  </what-built>
  <how-to-verify>
    Run the app: cd app && python -m uvicorn app.server:app --reload (or launch the built app)
    Then open http://localhost:7433 in a browser.

    Full automated suite first:
    - cd app && python -m pytest tests/ -q — confirm all GREEN
    - cd app/frontend && npm run build — confirm exits 0

    Then verify these behaviors:

    1. Switch to an experiment branch (e.g., experiment/2026-03-15-price-calc-test):
       - In Remote panel GitHub tab, confirm "Open pull request" button appears below push controls
       - Confirm the button is NOT visible in GitLab tab if GitLab is not connected (or vice versa)

    2. Click "Open pull request":
       - Confirm inline form expands (no browser redirect)
       - Confirm title field is pre-filled with "Price calc test" (humanized from branch name)
       - Confirm Create button is disabled when title is cleared
       - Confirm typing a description is optional (form can be submitted with only title)

    3. Click Create:
       - Confirm a PR is created on GitHub (or confirm the API call fires in browser DevTools Network tab if you don't have a connected repo)
       - Confirm success state shows "✓ Pull request created. View on GitHub →" link inline
       - Confirm the link opens the correct GitHub PR page when clicked (if real PR created)

    4. Refresh the panel / switch away and back:
       - Confirm "View pull request →" link appears instead of the create button (existing PR detected)

    5. Switch to main branch:
       - Confirm the PR button section is absent entirely

    6. GitLab tab:
       - Confirm "Open merge request" wording (not "pull request")
       - Confirm identical form behavior

    Note: If no live GitHub/GitLab connection is available, verify steps 1, 2, 5, and 6 visually. Verify step 3 via DevTools Network (POST /api/remote/pr/create fires with correct payload).
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors verified, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirmed all Phase 18.1 behaviors work end-to-end:
- PR button visible on experiment branch, absent on main
- Inline form with humanized pre-filled title
- Create button disabled with empty title
- Success state with correct platform link
- Existing PR detection shows "View pull request" on reload
- GitLab tab uses "merge request" label
- pytest tests/ GREEN, frontend builds with zero errors
</verification>

<success_criteria>
Human approval ("approved") with no blocking issues reported.
</success_criteria>

<output>
After human approval, create `.planning/phases/18.1-creation-of-pr/18.1-04-SUMMARY.md`
</output>
