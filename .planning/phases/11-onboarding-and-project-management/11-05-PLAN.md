---
phase: 11-onboarding-and-project-management
plan: "05"
type: execute
wave: 4
depends_on: ["11-04"]
files_modified: []
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04]

must_haves:
  truths:
    - "First-time user (no config.json) sees WelcomeScreen before any other UI"
    - "Clicking the CTA button opens the OS native folder picker"
    - "After selecting a folder with no git history, a pre-action confirmation dialog appears ('Set up version control?') with [Cancel] and [Set Up] buttons — git init only runs if the user clicks Set Up"
    - "After adding a folder with existing git history, no dialog appears — project is added silently"
    - "After adding a folder, git identity card appears if name/email not configured"
    - "After saving git identity, EmptyState guidance panel appears"
    - "Sidebar shows all registered projects; clicking switches active project"
    - "Right-clicking a sidebar project shows 'Remove project'; confirm removes it from list, files untouched"
    - "Multiple projects can be registered and switched between"
  artifacts:
    - path: "app/frontend/dist/"
      provides: "Built React app — served by FastAPI"
  key_links:
    - from: "WelcomeScreen CTA"
      to: "AppShell sidebar"
      via: "project added → projects.length > 0 → WelcomeScreen unmounts, AppShell mounts"
      pattern: "conditional render"
---

<objective>
Human verification of the complete Phase 11 onboarding and project management flow. Confirm all four requirements (ONBOARD-01 through ONBOARD-04) work end-to-end in a running app before marking the phase complete.

Purpose: Automated tests cover the backend API; this checkpoint verifies the full frontend UX flow that cannot be automated.
Output: Human approval that the onboarding flow is functional and matches the CONTEXT.md decisions.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-onboarding-and-project-management/11-CONTEXT.md
@.planning/phases/11-onboarding-and-project-management/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build frontend and start the server</name>
  <files></files>
  <action>
Build the frontend and start the full app so the human can verify the UX flow:

```bash
cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff

# 1. Build the React frontend
cd app/frontend && npm run build && cd ../..

# 2. Run the full test suite one final time
pytest tests/ -x -q

# 3. Start the server (keep running for manual verification)
python -m uvicorn app.server:app --port 7433
```

The server will open at http://localhost:7433.

**To simulate first-run (no projects registered):** Delete or rename the AlteryxGitCompanion config.json before starting:
- macOS: `~/Library/Application Support/AlteryxGitCompanion/config.json`
- Windows: `%APPDATA%\Local\AlteryxGitCompanion\config.json`
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && pytest tests/ -x -q && echo "All tests green"</automated>
  </verify>
  <done>All tests green; server starts; http://localhost:7433 accessible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete onboarding flow end-to-end</name>
  <files></files>
  <action>Human verifies the complete onboarding UX flow per the steps below. No automated action — this is a manual verification step.</action>
  <verify>
    <automated>MISSING — manual verification only; human confirms all 5 steps pass</automated>
  </verify>
  <done>Human types "approved" after confirming all 5 UX steps work correctly</done>
  <what-built>
    Complete Phase 11 onboarding and project management flow:
    - WelcomeScreen (ONBOARD-01)
    - Add folder with pre-confirmation git init dialog (ONBOARD-02)
    - Git identity prompt on first add (ONBOARD-03)
    - Project list sidebar with switch + remove (ONBOARD-04)
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:7433 — confirm WelcomeScreen appears (centered card with 4 bullets and "Add Your First Folder" button). [ONBOARD-01]

    2. Click "Add Your First Folder" — confirm OS native folder picker opens. Select a folder that has NO git history. Confirm a dialog appears BEFORE any operation runs: "Set up version control?" with a plain-language description ("This folder isn't a git repo yet. We'll set it up for version control. Continue?") and two buttons: [Cancel] and [Set Up]. Click Cancel — confirm nothing happens and you return to WelcomeScreen. Click "Add Your First Folder" again, select the same folder, this time click [Set Up] — confirm the project is added. [ONBOARD-02]

    3. After adding the folder: if your machine has NO global git user.name/email configured, confirm an "Almost done" card appears with Name + Email fields. Fill them in and click Save. Confirm the card disappears and an empty-state panel appears: "No saved versions yet". [ONBOARD-03]

    4. Click the '+' icon at the top of the sidebar. Add a second folder that already HAS git history. Confirm the project is added silently — NO dialog of any kind appears. Confirm both projects appear in the sidebar by basename only (no full path shown). Click between them — confirm active project highlights. [ONBOARD-04]

    5. Right-click a project in the sidebar — confirm "Remove project" appears in the context menu. Click it — confirm a confirmation dialog appears with reassuring copy ("files are not deleted"). Confirm removal — project disappears from sidebar. Files should still exist on disk. [ONBOARD-04]

    Report any issues with the exact behavior, copy, or interactions.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 steps pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
All automated tests pass + human approval received.
</verification>

<success_criteria>
- Human approves all 5 verification steps
- ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04 visually confirmed working
- Pre-confirmation dialog behavior verified: Cancel aborts, Set Up proceeds; git repos added silently
- No blocking UX issues reported
</success_criteria>

<output>
After completion, create `.planning/phases/11-onboarding-and-project-management/11-05-SUMMARY.md`
</output>
