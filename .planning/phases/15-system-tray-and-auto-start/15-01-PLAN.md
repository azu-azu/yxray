---
phase: 15-system-tray-and-auto-start
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_autostart.py
  - tests/test_settings.py
  - tests/test_tray.py
  - tests/test_main.py
autonomous: true
requirements:
  - APP-02
  - APP-05

must_haves:
  truths:
    - "All four test files exist with failing test stubs (RED state)"
    - "pytest discovers all test functions and reports them as FAILED (not ERROR)"
    - "Full test suite (156 existing) still passes with new stubs added"
  artifacts:
    - path: "tests/test_autostart.py"
      provides: "Failing stubs for autostart service (register/unregister/is_enabled)"
      exports: ["test_register_writes_run_key", "test_is_enabled_false", "test_unregister_removes_key"]
    - path: "tests/test_settings.py"
      provides: "Failing stubs for GET/POST /api/settings router"
      exports: ["test_get_settings", "test_post_settings_enable", "test_post_settings_disable"]
    - path: "tests/test_tray.py"
      provides: "Failing stubs for tray state computation"
      exports: ["test_state_changes_detected", "test_state_watching", "test_state_idle"]
    - path: "tests/test_main.py"
      provides: "Failing stubs for --background flag and second-instance detection"
      exports: ["test_background_flag_suppresses_browser", "test_is_instance_running_true", "test_is_instance_running_false"]
  key_links:
    - from: "tests/test_autostart.py"
      to: "app/services/autostart.py"
      via: "import (module will not exist until Plan 02)"
      pattern: "from app.services import autostart"
    - from: "tests/test_settings.py"
      to: "app/routers/settings.py"
      via: "import (module will not exist until Plan 03)"
      pattern: "from app.routers import settings"
    - from: "tests/test_tray.py"
      to: "app/tray.py"
      via: "import (module will not exist until Plan 03)"
      pattern: "from app.tray import _compute_state"
---

<objective>
Create four failing test files (RED state) covering all Phase 15 requirements. These stubs establish the test contracts that Plans 02 and 03 must satisfy.

Purpose: Nyquist compliance — every implementation task runs an automated verify command against pre-existing tests.
Output: tests/test_autostart.py, tests/test_settings.py, tests/test_tray.py, tests/test_main.py (all failing, none ERROR).
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-system-tray-and-auto-start/15-CONTEXT.md
@.planning/phases/15-system-tray-and-auto-start/15-RESEARCH.md
@.planning/phases/15-system-tray-and-auto-start/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for autostart service and main.py</name>
  <files>tests/test_autostart.py, tests/test_main.py</files>
  <behavior>
    test_autostart.py — all tests import from app.services.autostart (does not exist yet → ImportError on collection, acceptable RED):
    - test_register_writes_run_key: mock winreg.CreateKey + winreg.SetValueEx; call register_autostart(); assert SetValueEx called with key name "Alteryx Git Companion" and value containing "--background"
    - test_is_enabled_false: mock winreg.OpenKey raising FileNotFoundError; assert is_autostart_enabled() returns False
    - test_is_enabled_true: mock winreg.OpenKey + winreg.QueryValueEx returning a value; assert is_autostart_enabled() returns True
    - test_unregister_removes_key: mock winreg.OpenKey + winreg.DeleteValue; call unregister_autostart(); assert DeleteValue called with "Alteryx Git Companion"
    - test_unregister_already_absent: mock winreg.OpenKey raising FileNotFoundError; assert unregister_autostart() returns True (already absent = success)
    - test_non_windows_returns_false: patch sys.platform to "linux"; assert register_autostart() returns False, is_autostart_enabled() returns False

    test_main.py — tests import from app.main (exists, but new functions not yet present → AttributeError on RED):
    - test_background_flag_suppresses_browser: patch sys.argv with ["app", "--background"], mock webbrowser.open, mock find_available_port, mock uvicorn.Server; call main(); assert webbrowser.open NOT called
    - test_is_instance_running_true: patch socket.socket to raise OSError on bind; assert is_instance_running() returns True
    - test_is_instance_running_false: patch socket.socket to succeed on bind; assert is_instance_running() returns False
  </behavior>
  <action>
    Write tests/test_autostart.py with the stubs above. Use unittest.mock.patch throughout. Use pytest parametrize where sensible.

    Key mock targets (will become real in Plan 02):
    - patch "winreg" as a module attribute inside the function (import is deferred — patch at sys.modules level using unittest.mock.MagicMock() registered in sys.modules["winreg"])
    - patch sys.platform for cross-platform test

    Write tests/test_main.py with the stubs above. For is_instance_running tests, import the function directly after it exists; for now the file may use pytest.importorskip or simply import and let AttributeError produce FAILED.

    Both files MUST be syntactically valid Python. Tests must be FAILED (assertion failures / import errors) not syntax ERROR.
  </action>
  <verify>
    <automated>python -m pytest tests/test_autostart.py tests/test_main.py --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports FAILED or ERROR for tests in both files; no syntax errors; existing 156 tests unaffected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs for settings router and tray state</name>
  <files>tests/test_settings.py, tests/test_tray.py</files>
  <behavior>
    test_settings.py — imports from app.routers.settings (does not exist yet):
    - test_get_settings_returns_enabled_state: mock autostart.is_autostart_enabled() returning True; GET /api/settings; assert 200 + {"launch_on_startup": True}
    - test_get_settings_returns_disabled_state: mock returning False; GET /api/settings; assert {"launch_on_startup": False}
    - test_post_settings_enable: POST /api/settings {"launch_on_startup": true}; assert autostart.register_autostart() called once
    - test_post_settings_disable: POST /api/settings {"launch_on_startup": false}; assert autostart.unregister_autostart() called once

    test_tray.py — imports _compute_state from app.tray (does not exist yet):
    - test_state_changes_detected: status_data = {"proj1": {"changed_count": 3}}; assert _compute_state(status_data) == ("changes", "Alteryx Git Companion — 3 changes detected")
    - test_state_watching: status_data = {"proj1": {"changed_count": 0}}; assert _compute_state(status_data) == ("watching", "Alteryx Git Companion — watching")
    - test_state_idle: status_data = {}; assert _compute_state(status_data) == ("idle", "Alteryx Git Companion")
    - test_state_multiple_projects_aggregates: status_data = {"p1": {"changed_count": 1}, "p2": {"changed_count": 2}}; assert _compute_state(status_data)[0] == "changes" and "3 changes" in _compute_state(status_data)[1]
    - test_state_singular_change: status_data = {"p1": {"changed_count": 1}}; assert "1 change detected" in _compute_state(status_data)[1] (singular, not "changes")
  </behavior>
  <action>
    Write tests/test_settings.py using FastAPI TestClient from starlette.testclient. Use unittest.mock.patch to mock autostart module. Follow the module-level import + patch pattern from Phases 11–14 (patch "app.routers.settings.autostart").

    Write tests/test_tray.py importing _compute_state directly from app.tray. No pystray or OS dependency — _compute_state is a pure function that takes a dict and returns (state_str, tooltip_str).

    Both files MUST be syntactically valid. Tests MUST be FAILED not ERROR where possible — wrap imports in try/except ImportError that causes pytest.fail() if the module is missing, so tests report FAILED rather than collection ERROR.

    Preferred pattern for graceful RED:
    ```python
    try:
        from app.tray import _compute_state
    except ImportError:
        _compute_state = None

    def test_state_idle():
        if _compute_state is None:
            pytest.fail("app.tray._compute_state not implemented yet")
        ...
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_settings.py tests/test_tray.py --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports FAILED for all stubs; no syntax errors; full suite still collects and the 156 passing tests remain green</done>
</task>

</tasks>

<verification>
Run full suite to confirm existing tests unbroken:
```
python -m pytest -x -q --ignore=tests/test_autostart.py --ignore=tests/test_settings.py --ignore=tests/test_tray.py --ignore=tests/test_main.py 2>&1 | tail -5
```
All 4 new stub files exist and are syntactically valid.
</verification>

<success_criteria>
- tests/test_autostart.py: 6 test functions, all FAILED
- tests/test_settings.py: 4 test functions, all FAILED
- tests/test_tray.py: 5 test functions, all FAILED
- tests/test_main.py: 3 test functions, all FAILED
- Existing 156 tests: still GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/15-system-tray-and-auto-start/15-01-SUMMARY.md`
</output>
