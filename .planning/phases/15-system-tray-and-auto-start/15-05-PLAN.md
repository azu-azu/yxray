---
phase: 15-system-tray-and-auto-start
plan: 05
type: execute
wave: 3
depends_on:
  - 15-04
files_modified: []
autonomous: false
requirements:
  - APP-02
  - APP-05

must_haves:
  truths:
    - "System tray icon appears in Windows taskbar area after running the app"
    - "Left-clicking the tray icon opens the browser at localhost:PORT"
    - "Right-clicking shows 'Open Alteryx Git Companion' and 'Quit' menu entries"
    - "Tray tooltip text changes when changes are detected"
    - "Gear icon appears at the bottom of the Sidebar"
    - "Clicking gear opens Settings panel with Launch on startup toggle"
    - "Full pytest suite is GREEN before handoff"
  artifacts:
    - path: "tests/test_autostart.py"
      provides: "All tests GREEN"
    - path: "tests/test_settings.py"
      provides: "All tests GREEN"
    - path: "tests/test_tray.py"
      provides: "All tests GREEN"
    - path: "tests/test_main.py"
      provides: "All tests GREEN"
  key_links:
    - from: "Phase 15"
      to: "Phase 16"
      via: "System tray active, auto-start registered — Phase 16 can add remote auth settings to existing settings panel"
      pattern: "SettingsPanel"
---

<objective>
Human verification checkpoint: confirm that all automated tests pass and the Phase 15 UI + system tray work correctly on Windows. No new code is written — this plan gates Phase 15 completion.

Purpose: Phase gate — validate APP-02 (auto-start), APP-04b (tray click opens browser), APP-05 (tray states + settings panel) end-to-end.
Output: Human sign-off that all Phase 15 behaviors work.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-system-tray-and-auto-start/15-CONTEXT.md
@.planning/phases/15-system-tray-and-auto-start/15-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm all Phase 15 tests GREEN</name>
  <files></files>
  <action>
    Run the complete test suite and confirm all tests pass — including the 4 new Phase 15 test files.

    Commands:
    ```
    python -m pytest tests/test_autostart.py tests/test_settings.py tests/test_tray.py tests/test_main.py -v
    python -m pytest -x -q
    ```

    If any tests are failing, diagnose and fix before proceeding to the checkpoint. Do not advance to human verification with red tests.
  </action>
  <verify>
    <automated>python -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass including 4 new Phase 15 test files; full suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — tray icon, settings panel, Registry key</name>
  <action>
    Pause for human to verify Phase 15 behaviors manually on Windows. See how-to-verify section below.
  </action>
  <verify>
    <automated>MISSING — manual-only: system tray requires Windows display + interactive UI</automated>
  </verify>
  <done>Human types "approved" confirming tray icon, background mode, settings toggle, and Registry key all work</done>
  <what-built>
    Full Phase 15 implementation:
    - Silent background mode (--background flag suppresses browser open on boot)
    - Auto-start registration in HKCU Registry Run key on first launch
    - System tray icon (idle / watching / changes-detected states via 5-second polling)
    - Tray left-click opens browser; right-click shows Open + Quit
    - Quit from tray sets server.should_exit = True (clean shutdown)
    - Settings gear icon in Sidebar → Settings panel → "Launch on startup" toggle
    - app.spec flipped to console=False
  </what-built>
  <how-to-verify>
    Run the app manually (development mode, not bundled):
    ```
    python -m app.main
    ```
    1. App starts, browser opens (no --background flag) — existing behavior preserved
    2. System tray icon appears in Windows taskbar notification area (bottom-right)
    3. Hover over tray icon — tooltip shows "Alteryx Git Companion" (idle) or "... — watching" if project registered
    4. Left-click tray icon → browser opens at localhost:PORT
    5. Right-click tray icon → context menu shows "Open Alteryx Git Companion" and "Quit"
    6. Click "Quit" → process exits (verify in Task Manager)

    Test background mode:
    ```
    python -m app.main --background
    ```
    7. No browser opens
    8. Tray icon still appears
    9. App still accessible at localhost:PORT via browser

    Test settings panel in browser UI:
    10. Open app in browser
    11. Gear icon at bottom of left sidebar is visible
    12. Click gear → Settings panel appears in main content area
    13. "Launch on startup" toggle visible with label
    14. Toggle ON → Registry key appears in regedit at HKCU\Software\Microsoft\Windows\CurrentVersion\Run with value ending in --background
    15. Toggle OFF → Registry key removed

    Test tray state changes (requires registered project):
    16. Register a project folder
    17. Tray tooltip updates to "watching" within 5 seconds
    18. Modify a .yxmd file → within 5 seconds tray shows "1 change detected" and icon turns amber
  </how-to-verify>
  <resume-signal>
    Type "approved" when all checked behaviors work, or describe what is broken so fixes can be applied.
  </resume-signal>
</task>

</tasks>

<verification>
All Phase 15 requirements verified:
- APP-02: auto-start registration + --background silent mode
- APP-04b: tray left-click opens browser
- APP-05: three tray states + settings toggle
</verification>

<success_criteria>
- Full pytest suite: GREEN (170+ tests)
- Human verified: tray icon visible, states correct, gear/settings panel functional
- Registry key written/removed correctly by toggle
- Phase 15 COMPLETE → unblock Phase 16
</success_criteria>

<output>
After completion, create `.planning/phases/15-system-tray-and-auto-start/15-05-SUMMARY.md`
</output>
