---
phase: 15-system-tray-and-auto-start
plan: 02
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - app/services/autostart.py
  - app/main.py
  - app.spec
autonomous: true
requirements:
  - APP-02

must_haves:
  truths:
    - "register_autostart() writes HKCU Run key containing --background flag (mocked on non-Windows)"
    - "is_autostart_enabled() returns True when key present, False when absent"
    - "unregister_autostart() deletes the Run key entry (idempotent — absent key returns True)"
    - "main() with --background in sys.argv does NOT call webbrowser.open"
    - "main() without --background calls webbrowser.open (existing Phase 10 behavior preserved)"
    - "is_instance_running() returns True when port 7433 already bound, False when free"
    - "Second instance with manual launch opens browser to running instance and exits"
    - "First launch registers autostart silently (no crash on registry write failure)"
  artifacts:
    - path: "app/services/autostart.py"
      provides: "register_autostart(), unregister_autostart(), is_autostart_enabled()"
      exports: ["register_autostart", "unregister_autostart", "is_autostart_enabled"]
    - path: "app/main.py"
      provides: "is_instance_running(), --background flag handling, tray thread launch stub, autostart registration on first launch"
      exports: ["main", "find_available_port", "is_instance_running"]
    - path: "app.spec"
      provides: "console=False + pystray/Pillow hiddenimports + icon assets in datas"
      contains: "console=False"
  key_links:
    - from: "app/main.py"
      to: "app/services/autostart.py"
      via: "from app.services import autostart; autostart.register_autostart()"
      pattern: "autostart\\.register_autostart"
    - from: "app/main.py"
      to: "app/tray.py"
      via: "from app import tray; threading.Thread(target=tray.start_tray, ...)"
      pattern: "tray\\.start_tray"
    - from: "app.spec"
      to: "assets/icon*.ico"
      via: "datas list includes icon.ico, icon-watching.ico, icon-changes.ico"
      pattern: "icon.*\\.ico"
---

<objective>
Implement the autostart service and update main.py with silent-background-mode support. Tests go GREEN for APP-02. Also flip app.spec to console=False and add pystray assets.

Purpose: Core auto-start requirement — app registers itself in HKCU Run key on first launch, respects --background flag to suppress browser open, and detects second instances via port probe.
Output: app/services/autostart.py (new), app/main.py (modified), app.spec (console=False).
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-system-tray-and-auto-start/15-CONTEXT.md
@.planning/phases/15-system-tray-and-auto-start/15-RESEARCH.md
@.planning/phases/15-system-tray-and-auto-start/15-01-SUMMARY.md

@app/main.py
@app/server.py
</context>

<interfaces>
<!-- Key contracts the executor must implement against -->

From tests/test_autostart.py (written in Plan 01):
```python
# Function signatures that must be satisfied:
from app.services import autostart

autostart.register_autostart() -> bool        # True on success, False on fail (silent)
autostart.unregister_autostart() -> bool      # True on success or already-absent
autostart.is_autostart_enabled() -> bool      # True if HKCU Run key present

# Registry constants:
_RUN_KEY = r"Software\Microsoft\Windows\CurrentVersion\Run"
_APP_NAME = "Alteryx Git Companion"
# Value written: f'"{exe_path}" --background'
```

From tests/test_main.py (written in Plan 01):
```python
from app.main import is_instance_running, main, find_available_port

is_instance_running() -> bool   # True if port 7433 already bound
# main() checks sys.argv for "--background"
# main() checks is_instance_running() first; if True + not background → webbrowser.open + sys.exit(0)
```

Existing app/main.py exports:
```python
def find_available_port(start: int = 7433, count: int = 11) -> tuple[int, socket.socket]
def main() -> None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/services/autostart.py → test_autostart.py GREEN</name>
  <files>app/services/autostart.py</files>
  <behavior>
    - register_autostart() on win32: winreg.CreateKey(HKEY_CURRENT_USER, _RUN_KEY) → SetValueEx(key, "Alteryx Git Companion", 0, REG_SZ, value) where value = f'"{exe}" --background'; returns True; OSError → log warning → return False
    - register_autostart() on non-win32: returns False immediately (no winreg import)
    - is_autostart_enabled() on win32: OpenKey + QueryValueEx; FileNotFoundError → False; OSError → False
    - unregister_autostart() on win32: OpenKey(KEY_SET_VALUE) + DeleteValue; FileNotFoundError → True (already absent); OSError → log warning → return False
    - _get_exe_path(): if sys.frozen → f'"{sys.executable}" --background'; else → f'"{sys.executable}" -m app.main --background'
    - winreg import MUST be deferred inside function body (never at module level) — macOS CI compatibility
  </behavior>
  <action>
    Create app/services/autostart.py following Pattern 4 from RESEARCH.md exactly.

    Key rules:
    - `import winreg` must be INSIDE each function body, after the `sys.platform != "win32"` guard returns False. Never at module level.
    - Use context managers (`with winreg.CreateKey(...) as key:`) for all registry operations.
    - _get_exe_path() uses `getattr(sys, "frozen", False)` to detect PyInstaller bundle.
    - Logger: `logger = logging.getLogger(__name__)` at module level.
    - Silent fail on OSError: log warning, return False (never raise).
    - Return type annotations: all three public functions return `bool`.

    Run tests after writing:
    `python -m pytest tests/test_autostart.py -x -q`
    All should pass (winreg is mocked via sys.modules patching in test stubs).
  </action>
  <verify>
    <automated>python -m pytest tests/test_autostart.py -x -q</automated>
  </verify>
  <done>All test_autostart.py tests GREEN; no import errors on macOS (winreg deferred)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update app/main.py with background mode + second-instance detection → test_main.py GREEN; update app.spec</name>
  <files>app/main.py, app.spec</files>
  <behavior>
    app/main.py additions:
    - is_instance_running() → bool: bind attempt to 127.0.0.1:7433; OSError = True (running); success (close socket) = False
    - main() updated:
      1. background_mode = "--background" in sys.argv
      2. is_instance_running() check: if True and not background_mode → webbrowser.open(f"http://localhost:7433") + sys.exit(0); if True and background_mode → sys.exit(0) (silent)
      3. port, sock = find_available_port() (unchanged)
      4. autostart registration: from app.services import autostart; autostart.register_autostart() — called once, result ignored
      5. Tray thread start: `from app import tray` + `tray_thread = threading.Thread(target=tray.start_tray, args=(port, server), daemon=True)` — note: server ref passed after creation
      6. Browser open timer: ONLY if not background_mode (existing behavior)
      7. uvicorn.Server created and stored: `server = uvicorn.Server(config)` — server ref used by tray for quit
      8. Tray thread started BEFORE asyncio.run(server.serve(...)) so tray is live during server run
      9. print() removed (console=False makes it invisible; logging via uvicorn log_level)
    - ORDER in main(): background_mode → is_instance_running → find_available_port → register_autostart → create server → start_tray_thread → optional browser timer → asyncio.run(server.serve())

    app.spec changes:
    - `console=True` → `console=False` (line 54)
    - Add to `excludes` removal: remove 'PIL' from excludes (Pillow now needed for pystray)
    - Add to `hiddenimports`: 'pystray', 'PIL', 'PIL.Image', 'PIL.ImageDraw'
    - Add to `datas`:
      - `('assets/icon.ico', 'assets')` — idle state icon
      - `('assets/icon-watching.ico', 'assets')` — watching state (will be generated in Plan 03)
      - `('assets/icon-changes.ico', 'assets')` — changes state (will be generated in Plan 03)
  </behavior>
  <action>
    Update app/main.py. Read the existing file first (already in context). Preserve all existing code — add is_instance_running() function and update main() following the ordering above.

    The `from app import tray` import inside main() is acceptable (lazy import avoids circular issues and keeps tray optional at module level). At this point app/tray.py does not exist — this is fine because tests mock it out. The import only executes at runtime when main() is called.

    Update app.spec: flip console=True → console=False. Remove 'PIL' from excludes list. Add pystray and PIL to hiddenimports. Add three asset datas entries. Do not change any other spec fields.

    Run tests after writing:
    `python -m pytest tests/test_main.py -x -q`
    `python -m pytest tests/test_autostart.py -x -q` (regression check)
  </action>
  <verify>
    <automated>python -m pytest tests/test_main.py tests/test_autostart.py -x -q</automated>
  </verify>
  <done>All test_main.py and test_autostart.py tests GREEN; app.spec has console=False; full suite green (python -m pytest -x -q)</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```
python -m pytest -x -q
```
Confirm app.spec has console=False:
```
grep "console=" app.spec
```
</verification>

<success_criteria>
- tests/test_autostart.py: all GREEN
- tests/test_main.py: all GREEN
- app/services/autostart.py: exists, no module-level winreg import
- app/main.py: has is_instance_running(), --background logic, tray thread start
- app.spec: console=False, pystray in hiddenimports, three icon assets in datas
- Full 156+ test suite: GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/15-system-tray-and-auto-start/15-02-SUMMARY.md`
</output>
