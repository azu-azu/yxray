---
phase: 18-ci-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - ci-templates/.github/workflows/pr-diff-report.yml
  - ci-templates/.github/scripts/generate_diff_comment.py
  - ci-templates/.gitlab-ci.yml
  - ci-templates/.gitlab/scripts/generate_diff_comment.py
  - ci-templates/README.md
autonomous: true
requirements:
  - CI-04

must_haves:
  truths:
    - "User can copy ci-templates/ into their own repo and configure CI without reading source code"
    - "ci-templates/ contains all required files in the exact directory structure"
    - "README.md gives step-by-step instructions for GitHub Actions and GitLab CI at equal depth"
    - "ci-templates/ files are repo-agnostic (no hardcoded org/repo names specific to the live example)"
  artifacts:
    - path: "ci-templates/.github/workflows/pr-diff-report.yml"
      provides: "Distributable GitHub Actions workflow — repo-agnostic copy"
      contains: "find-or-update"
    - path: "ci-templates/.github/scripts/generate_diff_comment.py"
      provides: "Distributable GitHub Python helper — identical to updated live example"
      contains: "is_private_repo"
    - path: "ci-templates/.gitlab-ci.yml"
      provides: "Cleaned GitLab CI — no test-job, only diff stage"
      contains: "stages:"
    - path: "ci-templates/.gitlab/scripts/generate_diff_comment.py"
      provides: "Distributable GitLab Python helper — copy of live example"
      contains: "artifact_url"
    - path: "ci-templates/README.md"
      provides: "Setup guide for non-technical Alteryx analysts"
      contains: "GITHUB_TOKEN"
  key_links:
    - from: "ci-templates/README.md"
      to: "ci-templates/.github/workflows/pr-diff-report.yml"
      via: "step-by-step copy instructions referencing the exact file path"
      pattern: ".github/workflows"
    - from: "ci-templates/README.md"
      to: "ci-templates/.gitlab-ci.yml"
      via: "step-by-step copy instructions referencing the exact file path"
      pattern: ".gitlab-ci.yml"
---

<objective>
Assemble the ci-templates/ distributable package: copy all updated CI files into the mirrored directory structure, adapt them to be repo-agnostic, and write the README for non-technical Alteryx analysts.

Purpose: Ship the polished CI templates so Alteryx users can set up automated diff reports in their own repos by copying one directory and following the README.
Output: ci-templates/ directory with 5 files; README covering GitHub Actions and GitLab CI setup end-to-end.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-ci-polish/18-CONTEXT.md
@.planning/phases/18-ci-polish/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ci-templates/ directory with all CI files (repo-agnostic copies)</name>
  <files>
    ci-templates/.github/workflows/pr-diff-report.yml,
    ci-templates/.github/scripts/generate_diff_comment.py,
    ci-templates/.gitlab-ci.yml,
    ci-templates/.gitlab/scripts/generate_diff_comment.py
  </files>
  <action>
Create the ci-templates/ directory structure in the alteryx_diff repo root with 4 CI files. All files are copies of the updated live-example files with repo-specific references replaced by generic placeholders.

**Directory structure to create:**
```
ci-templates/
├── .github/
│   ├── workflows/
│   │   └── pr-diff-report.yml
│   └── scripts/
│       └── generate_diff_comment.py
├── .gitlab-ci.yml
└── .gitlab/
    └── scripts/
        └── generate_diff_comment.py
```

**File 1: ci-templates/.github/workflows/pr-diff-report.yml**
Copy from /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml (the updated version from Plan 02). Make these adaptations:
- Replace the header comment block to remove repo-specific notes. Add at the top:
  ```yaml
  # ─────────────────────────────────────────────────────────────────────────────
  # ci-templates/.github/workflows/pr-diff-report.yml
  #
  # SETUP: Copy this file to .github/workflows/ in your Alteryx workflow repo.
  # See ci-templates/README.md for full setup instructions.
  #
  # Trigger: Every PR that touches a .yxmd or .yxwz file.
  # ─────────────────────────────────────────────────────────────────────────────
  ```
- In the "Install acd" step, keep the `pip install git+https://github.com/Laxmi884/alteryx_diff.git` line but add an inline comment:
  `# TODO: Replace with: pip install alteryx-canvas-diff  (once published to PyPI)`
- Remove the "NOTE: htmlpreview.github.io" comment block at the top (no longer accurate after CI-02 changes)
- All other content (find-or-update JS, GITHUB_TOKEN env, upload-artifact step) copied exactly as-is

**File 2: ci-templates/.github/scripts/generate_diff_comment.py**
Copy /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py verbatim. No adaptations needed — the script uses only env vars (no hardcoded org/repo names).

**File 3: ci-templates/.gitlab-ci.yml**
Copy /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml (the cleaned version from Plan 01, with test-job removed). Make these adaptations:
- Replace the header comment to add:
  ```yaml
  # ─────────────────────────────────────────────────────────────────────────────
  # ci-templates/.gitlab-ci.yml
  #
  # SETUP: Copy this file to your Alteryx workflow repo root as .gitlab-ci.yml.
  # See ci-templates/README.md for full setup instructions.
  # ─────────────────────────────────────────────────────────────────────────────
  ```
- In the pip install line inside the script block, add the same TODO comment:
  `# TODO: Replace with: pip install alteryx-canvas-diff  (once published to PyPI)`
- All other content copied exactly as-is

**File 4: ci-templates/.gitlab/scripts/generate_diff_comment.py**
Copy /Users/laxmikantmukkawar/alteryx/.gitlab/scripts/generate_diff_comment.py verbatim. No adaptations needed.

After creating all files, verify the directory structure is correct.
  </action>
  <verify>
    <automated>find /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff/ci-templates -type f | sort</automated>
  </verify>
  <done>find returns exactly 4 files: ci-templates/.github/workflows/pr-diff-report.yml, ci-templates/.github/scripts/generate_diff_comment.py, ci-templates/.gitlab-ci.yml, ci-templates/.gitlab/scripts/generate_diff_comment.py. grep "test-job" ci-templates/.gitlab-ci.yml returns empty. grep "listComments" ci-templates/.github/workflows/pr-diff-report.yml returns a match. grep "is_private_repo" ci-templates/.github/scripts/generate_diff_comment.py returns a match. diff /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py ci-templates/.github/scripts/generate_diff_comment.py returns empty (files are identical — verbatim copy confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: Write ci-templates/README.md — setup guide for non-technical Alteryx analysts (CI-04)</name>
  <files>ci-templates/README.md</files>
  <action>
Write ci-templates/README.md as a complete, self-contained setup guide. Target audience: non-technical Alteryx analysts who know how to copy files and follow step-by-step instructions but have no CI/CD experience.

**Formatting guidance (Claude's discretion):** Use a single README with clear H2 sections for GitHub and GitLab. Include a short "What this does" intro at the very top (2-3 sentences). Use numbered steps within each section for the copy-paste flow. Use code blocks for every command or file path the user must type/copy.

**Content requirements — cover all of the following:**

---

**Intro (top of file):**
- What this does: Automatically posts a diff report as a PR/MR comment whenever a workflow file changes in a pull request. Shows which tools were added, removed, or modified.
- Link to the acd repo for background.

---

**Section: GitHub Actions Setup**

Include all of these sub-sections in this order:

1. **Prerequisites**
   - A GitHub repository containing your Alteryx workflow files (.yxmd, .yxwz)
   - No Python installation required — GitHub Actions handles this automatically

2. **Step 1: Copy the template files**
   Exact instructions to copy these two files (with exact destination paths):
   - `ci-templates/.github/workflows/pr-diff-report.yml` → `.github/workflows/pr-diff-report.yml` in their repo
   - `ci-templates/.github/scripts/generate_diff_comment.py` → `.github/scripts/generate_diff_comment.py` in their repo
   Note: create `.github/workflows/` and `.github/scripts/` directories if they don't exist

3. **Step 2: Public vs Private repo — what changes**
   Two sub-paths:
   - **Public repo:** The `GITHUB_TOKEN` (automatic, no setup needed) covers everything. Report links are accessible without login.
   - **Private repo:** `GITHUB_TOKEN` covers posting comments. For the pip install of acd from GitHub, create a `GH_PAT` secret (fine-grained PAT, Contents: Read on the acd repo) and update the install line in the workflow to use `git+https://${{ secrets.GH_PAT }}@github.com/Laxmi884/alteryx_diff.git`. Include the exact edit to make with a code block.

4. **Step 3: GITHUB_TOKEN permissions**
   Explain that `GITHUB_TOKEN` is provided automatically by GitHub Actions — no manual setup needed. The workflow already includes `pull-requests: write` permission for posting comments.

5. **Step 4: Commit and push**
   Exact instruction: Commit both files and push to any branch. The workflow triggers automatically on PRs that touch `.yxmd` or `.yxwz` files.

6. **What you'll see**
   Describe the PR comment that appears: future pushes update the same comment instead of posting a new one. Per-file report table with links to the Actions run.

7. **Troubleshooting (GitHub)**
   - "No comment was posted": Check the Actions tab for the workflow run. Verify `pull-requests: write` permission is in the YAML.
   - "The workflow didn't trigger": Confirm the PR touches a `.yxmd` or `.yxwz` file. Check the `paths:` filter in the workflow.
   - "pip install failed": For private repos, confirm `GH_PAT` is set in repo Settings → Secrets and Variables → Actions.

---

**Section: GitLab CI Setup**

Include all of these sub-sections in this order:

1. **Prerequisites**
   - A GitLab project containing your Alteryx workflow files
   - A GitLab Project Access Token with `api` scope (for posting MR comments)

2. **Step 1: Create a Project Access Token (GITLAB_TOKEN)**
   Step-by-step with exact UI navigation:
   - Go to your GitLab project → Settings → Access Tokens
   - Click "Add new token"
   - Name: `acd-ci`, Expiry: (set a date), Role: Developer, Scope: `api`
   - Click "Create project access token" and copy the value immediately
   - Go to Settings → CI/CD → Variables → Add variable
   - Key: `GITLAB_TOKEN`, Value: (paste the token), check "Mask variable", click "Add variable"

3. **Step 2: Copy the template files**
   Exact instructions:
   - `ci-templates/.gitlab-ci.yml` → `.gitlab-ci.yml` in your repo root (if a `.gitlab-ci.yml` already exists, merge the `alteryx-diff` job into it — copy the `stages:` addition and the full `alteryx-diff:` job block)
   - `ci-templates/.gitlab/scripts/generate_diff_comment.py` → `.gitlab/scripts/generate_diff_comment.py` in your repo

4. **Step 3: How the artifact link works**
   Explain `expose_as: "Alteryx Diff Report"`: GitLab automatically adds a "View exposed artifact" button in the MR page. Clicking it opens the HTML report directly in the browser — no download or ZIP required. This works on private projects.

5. **Step 4: Commit and push**
   Same as GitHub instructions — commit both files, push, create an MR touching a `.yxmd` file to test.

6. **What you'll see**
   MR comment posted by the pipeline with the diff summary. "View exposed artifact" button in the MR for each HTML report.

7. **Troubleshooting (GitLab)**
   - "No comment was posted": Check that `GITLAB_TOKEN` is set in CI/CD Variables and has `api` scope.
   - "The pipeline didn't trigger": Confirm the MR touches a `.yxmd` or `.yxwz` file.
   - "View exposed artifact gives 404": Verify `paths: - "diff_reports/"` is present in the artifacts section and the Python script outputs to `diff_reports/`.

---

Keep the README to the point. Assume readers know how to open a file and paste text into it. Do NOT explain what git is, what pull requests are, or what CI/CD is in general.
  </action>
  <verify>
    <automated>wc -l /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff/ci-templates/README.md</automated>
  </verify>
  <done>ci-templates/README.md exists with at least 80 lines. Contains both "GitHub Actions" and "GitLab CI" section headings. Contains "GITLAB_TOKEN", "GITHUB_TOKEN", "expose_as", and "Project Access Token" strings. The file is readable markdown.</done>
</task>

</tasks>

<verification>
- `find ci-templates/ -type f | sort` → returns exactly 5 files (4 CI files + README.md)
- `grep -c "test-job" ci-templates/.gitlab-ci.yml` → 0
- `grep "listComments" ci-templates/.github/workflows/pr-diff-report.yml` → match found
- `grep "is_private_repo" ci-templates/.github/scripts/generate_diff_comment.py` → match found
- `grep "GITLAB_TOKEN" ci-templates/README.md` → match found
- `grep "GITHUB_TOKEN" ci-templates/README.md` → match found
- `grep "expose_as" ci-templates/README.md` → match found
- `diff /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py ci-templates/.github/scripts/generate_diff_comment.py` → empty (verbatim copy)
</verification>

<success_criteria>
- ci-templates/ exists with exactly 5 files in the correct mirrored directory structure
- All CI files are repo-agnostic (no hardcoded Laxmi884/alteryx workflows-specific repo URLs without TODO placeholder comments)
- README.md covers GitHub and GitLab setup at equal depth with step-by-step instructions sufficient for a non-technical Alteryx analyst
- ci-templates/.github/scripts/generate_diff_comment.py is byte-for-byte identical to the updated live example
- CI-04 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-ci-polish/18-03-SUMMARY.md`
</output>
