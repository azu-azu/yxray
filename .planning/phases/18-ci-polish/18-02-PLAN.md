---
phase: 18-ci-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py
  - /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml
autonomous: true
requirements:
  - CI-01
  - CI-02

must_haves:
  truths:
    - "Every generated PR comment body starts with <!-- acd-diff-report --> as its first line"
    - "On each push to an existing PR, the workflow updates the existing ACD comment instead of posting a new one"
    - "build_comment() returns a per-file table when html_count > 0"
    - "is_private_repo() returns the correct bool by calling the GitHub repos API"
    - "pytest tests/test_ci_github_comment.py passes (GREEN)"
  artifacts:
    - path: "/Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py"
      provides: "Updated Python helper with is_private_repo, per-file table, marker support, run_url param"
      exports: ["is_private_repo", "build_comment", "build_no_files_comment"]
    - path: "/Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml"
      provides: "Updated workflow with find-or-update comment step and GITHUB_TOKEN env for Python step"
      contains: "listComments"
  key_links:
    - from: "/Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml"
      to: "github.rest.issues.listComments"
      via: "actions/github-script@v7 inline JS"
      pattern: "listComments"
    - from: "/Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py"
      to: "https://api.github.com/repos/{repo}"
      via: "urllib.request in is_private_repo()"
      pattern: "is_private_repo"
---

<objective>
Update the GitHub Actions Python helper and workflow YAML to implement find-or-update comment (CI-01) and public/private-aware per-file HTML report table (CI-02). Turn the RED scaffold tests GREEN.

Purpose: The core CI polish work — one comment per PR, per-file report table, and auto-detection of repo visibility.
Output: Updated generate_diff_comment.py and pr-diff-report.yml; all 7 test cases pass.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-ci-polish/18-CONTEXT.md
@.planning/phases/18-ci-polish/18-RESEARCH.md
@.planning/phases/18-ci-polish/18-01-SUMMARY.md

<interfaces>
<!-- Key functions in generate_diff_comment.py (BEFORE changes — read this before editing) -->

From /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py:

```python
def actions_run_url() -> str:
    """Builds the Actions run URL from env vars GITHUB_RUN_ID, GITHUB_SERVER_URL, GITHUB_REPOSITORY."""

def build_comment(
    sections:     list[str],
    files:        list[str],
    html_count:   int,
    short_sha:    str,
    timestamp:    str,
    totals:       dict[str, int],
    errors:       int,
) -> str:
    """Assembles the full PR comment markdown. Currently calls actions_run_url() internally."""

def build_no_files_comment(short_sha: str, timestamp: str) -> str:
    """Returns a minimal comment for PRs with no Alteryx files changed."""
```

<!-- Current "Post comment" step in pr-diff-report.yml (Step 5) uses createComment only — replace this entire step -->
<!-- GITHUB_TOKEN env var is NOT currently passed to the "Generate diff comment" Python step — add it -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update generate_diff_comment.py — add is_private_repo, per-file table, marker, run_url param</name>
  <files>/Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py</files>
  <behavior>
    - is_private_repo() returns True when GITHUB_TOKEN or GITHUB_REPOSITORY env vars are missing
    - is_private_repo() returns False when urlopen returns {"private": false}
    - is_private_repo() returns True when urlopen raises any exception
    - build_comment(...) returned string starts with "<!-- acd-diff-report -->" on its first line
    - build_no_files_comment(...) returned string starts with "<!-- acd-diff-report -->" on its first line
    - build_comment(..., html_count=1, run_url="https://...") contains "| Workflow File |" table header
    - build_comment(..., html_count=0, ...) does NOT contain "| Workflow File |"
  </behavior>
  <action>
**PREFLIGHT: Before making any edits, verify the source file exists:**
```bash
test -f /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py && echo "EXISTS" || echo "NOT FOUND — stop and report path error"
```
If NOT FOUND, stop immediately and report the error. Do not attempt to create the file at an alternative path.

**CONTEXT.md specified `htmlpreview.github.io` links for public repos. RESEARCH.md found this is infeasible for workflow artifacts (GitHub artifact URLs require authentication regardless of repo visibility). Using the Actions run URL for both repo types instead, with visibility-appropriate messaging: "No login required" for public repos, "Login required to download" for private repos.**

Make these targeted changes to /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py:

**1. Add import at top** (after existing imports): `import urllib.request`

**2. Add is_private_repo() function** (after actions_run_url()):

```python
def is_private_repo() -> bool:
    """Returns True if the workflow repository is private, False if public.
    Defaults to True (conservative) on any error."""
    token = os.environ.get("GITHUB_TOKEN", "")
    repo  = os.environ.get("GITHUB_REPOSITORY", "")
    if not token or not repo:
        return True
    url = f"https://api.github.com/repos/{repo}"
    req = urllib.request.Request(url, headers={
        "Authorization": f"Bearer {token}",
        "Accept": "application/vnd.github+json",
        "X-GitHub-Api-Version": "2022-11-28",
    })
    try:
        with urllib.request.urlopen(req, timeout=5) as resp:
            data = json.loads(resp.read())
            return bool(data.get("private", True))
    except Exception as exc:
        print(f"    WARNING: visibility check failed ({exc}) — defaulting to private", flush=True)
        return True
```

**3. Update build_comment() signature** — add `run_url: str = ""` parameter (replaces the internal `actions_run_url()` call in html_block). The caller (main()) will pass `run_url=actions_run_url()` explicitly. This makes the function testable without env vars.

Also update the html_block generation inside build_comment() to use a per-file table. Replace the current html_block with:

```python
html_block: list[str] = []
if html_count > 0 and run_url:
    private = is_private_repo()
    note = "Login required to download" if private else "No login required"
    html_block = [
        "> ### 📄 Interactive HTML Reports",
        ">",
        "> | Workflow File | Report |",
        "> |--------------|--------|",
    ]
    for fname in files:
        html_block.append(f"> | `{fname}` | [View report ↗]({run_url}) |")
    html_block += [
        ">",
        f"> *({note} · Interactive workflow graph · per-tool field diffs)*",
        "",
    ]
```

**4. Add MARKER prefix to build_comment() and build_no_files_comment()**

**MARKER OWNERSHIP: Python functions own the marker. Prepend `"<!-- acd-diff-report -->\n"` to the returned string of both `build_comment()` and `build_no_files_comment()`. The JS step in the workflow reads diff_comment.md content as-is — it does NOT prepend the marker again. If JS also prepends, the comment will have two marker lines and the find-or-update detection (`body.includes(MARKER)`) may match the wrong comment or create duplicates. Single ownership: Python only.**

Add `"<!-- acd-diff-report -->\n"` prefix to the returned string of both build_comment() and build_no_files_comment().

**5. Update main()** — pass `run_url=actions_run_url()` to build_comment() call.

**Anti-patterns to avoid:**
- Do NOT call is_private_repo() outside of build_comment() — it makes a real HTTP call; keep it inside the html_block section only
- Do NOT change the function signatures of git_changed_files, git_base_bytes, run_acd_json, run_acd_html — other code depends on those
- Do NOT add any new pip dependencies — stdlib only (urllib.request is already stdlib)
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/test_ci_github_comment.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 tests in test_ci_github_comment.py pass (GREEN). generate_diff_comment.py has is_private_repo(), build_comment() signature includes run_url param, both comment builders prepend the marker.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Step 5 in pr-diff-report.yml to find-or-update pattern (CI-01)</name>
  <files>/Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml</files>
  <action>
Make two changes to /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml:

**Change 1: Add GITHUB_TOKEN to the "Generate diff comment" step (Step 4)**

The existing Step 4 is:
```yaml
- name: Generate diff comment
  run: python3 .github/scripts/generate_diff_comment.py
  env:
    BASE_REF: ${{ github.base_ref }}
    HEAD_SHA: ${{ github.event.pull_request.head.sha }}
```

Add GITHUB_TOKEN to the env block:
```yaml
- name: Generate diff comment
  run: python3 .github/scripts/generate_diff_comment.py
  env:
    BASE_REF: ${{ github.base_ref }}
    HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**Change 2: Replace Step 5 "Post diff report as PR comment" entirely**

Replace the current `createComment`-only step with the full find-or-update pattern. The MARKER is `<!-- acd-diff-report -->`. The JS step reads diff_comment.md (already contains the marker as first line from the Python helper). Do NOT prepend the marker again in JS — single ownership rule: Python helper owns the marker, JS reads file content as-is.

New Step 5:
```yaml
      # ── 5. Post or update diff report PR comment ───────────────────────────
      # Uses find-or-update pattern: embeds <!-- acd-diff-report --> marker
      # in the comment body so future pushes update the same comment rather
      # than creating a new one. per_page: 100 avoids first-page miss on
      # PRs with many comments (GitHub paginates at 30 by default).
      - name: Post or update diff report PR comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs   = require('fs');
            const path = require('path');

            const MARKER      = '<!-- acd-diff-report -->';
            const commentFile = path.join(process.env.GITHUB_WORKSPACE, 'diff_comment.md');
            const sha         = context.payload.pull_request.head.sha.substring(0, 7);
            const ts          = new Date().toUTCString();

            let body;
            if (fs.existsSync(commentFile)) {
              body = fs.readFileSync(commentFile, 'utf8');
              // body already starts with MARKER — Python helper owns the marker.
              // Do NOT prepend MARKER here; doing so causes double-marker and breaks detection.
            } else {
              body = [
                MARKER,
                '## Alteryx Workflow Diff Report',
                '',
                '> Could not generate diff report. Check the Actions log for details.',
                '',
                'Commit: `' + sha + '` | ' + ts,
              ].join('\n');
            }

            const { data: comments } = await github.rest.issues.listComments({
              owner:        context.repo.owner,
              repo:         context.repo.repo,
              issue_number: context.issue.number,
              per_page:     100,
            });

            const existing = comments.find(c => c.body && c.body.includes(MARKER));

            if (existing) {
              await github.rest.issues.updateComment({
                owner:      context.repo.owner,
                repo:       context.repo.repo,
                comment_id: existing.id,
                body,
              });
              core.info('Updated existing comment: ' + existing.html_url);
            } else {
              const result = await github.rest.issues.createComment({
                owner:        context.repo.owner,
                repo:         context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
              core.info('Created new comment: ' + result.data.html_url);
            }
```

Update the header comment block at the top of the file to reflect the change:
- Change "5. Post diff_comment.md as a new PR comment" to "5. Post or update diff report PR comment (find-or-update via <!-- acd-diff-report --> marker)"

No other changes to the workflow.
  </action>
  <verify>
    <automated>grep -c "listComments" /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml</automated>
  </verify>
  <done>grep -c "listComments" returns 1 or more. grep -c "createComment" returns 1 (in the else branch). The file is valid YAML (python3 -c "import yaml; yaml.safe_load(open('/Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml'))" exits 0). GITHUB_TOKEN appears in the Generate diff comment step env block.</done>
</task>

</tasks>

<verification>
- `cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/test_ci_github_comment.py -q` → all 7 tests pass
- `grep "listComments" /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml` → returns the line
- `grep "GITHUB_TOKEN" /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml` → appears in Generate diff comment env block
- `grep "acd-diff-report" /Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml` → appears in JS MARKER constant
- `python3 -c "import yaml; yaml.safe_load(open('/Users/laxmikantmukkawar/alteryx/.github/workflows/pr-diff-report.yml'))"` → exits 0
</verification>

<success_criteria>
- All 7 tests in test_ci_github_comment.py are GREEN (CI-01 and CI-02 unit tests passing)
- pr-diff-report.yml uses find-or-update pattern with per_page: 100 (CI-01)
- generate_diff_comment.py has is_private_repo() and per-file table in build_comment() (CI-02)
- Both comment builders prepend <!-- acd-diff-report --> marker (Python owns the marker — JS does not re-prepend)
- GITHUB_TOKEN passed to Python helper step
- YAML file is valid
</success_criteria>

<output>
After completion, create `.planning/phases/18-ci-polish/18-02-SUMMARY.md`
</output>
