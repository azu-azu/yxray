---
phase: 18-ci-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_ci_github_comment.py
  - /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml
autonomous: true
requirements:
  - CI-03

must_haves:
  truths:
    - "pytest tests/test_ci_github_comment.py fails with ImportError or assertion failures (RED state — file exists, tests fail)"
    - "GitLab CI config contains no test-job block and no test stage"
  artifacts:
    - path: "tests/test_ci_github_comment.py"
      provides: "RED test scaffold for CI-01 and CI-02 behaviours"
      contains: "def test_"
    - path: "/Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml"
      provides: "Cleaned GitLab CI with only diff stage"
      contains: "stages:\n  - diff"
  key_links:
    - from: "tests/test_ci_github_comment.py"
      to: "/Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py"
      via: "import generate_diff_comment — tests import from alteryx repo path"
      pattern: "import.*generate_diff_comment"
---

<objective>
Create a Wave 0 test scaffold for CI-01 and CI-02 helper functions (RED state — tests written before implementation), and remove the placeholder test-job from GitLab CI (CI-03 — one-line cleanup).

Purpose: Establish the Nyquist-compliant test baseline before updating the GitHub helper script in Plan 02. GitLab cleanup is fast and independent.
Output: tests/test_ci_github_comment.py (failing), cleaned /alteryx/.gitlab-ci.yml.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ci-polish/18-CONTEXT.md
@.planning/phases/18-ci-polish/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test scaffold for generate_diff_comment.py (CI-01, CI-02)</name>
  <files>tests/test_ci_github_comment.py</files>
  <action>
Create tests/test_ci_github_comment.py in the alteryx_diff repo. This file tests the GITHUB version of generate_diff_comment.py located at /Users/laxmikantmukkawar/alteryx/.github/scripts/generate_diff_comment.py.

Because the Python script is not installed as a package, use sys.path insertion to import it:

```python
import sys, os
sys.path.insert(0, "/Users/laxmikantmukkawar/alteryx/.github/scripts")
```

Then import: `import generate_diff_comment as gdc`

**MARKER OWNERSHIP: Python functions own the marker. Both `build_comment()` and `build_no_files_comment()` prepend `<!-- acd-diff-report -->` as the first line of their returned string. The JS step in the workflow reads the file content as-is — it does NOT prepend the marker again. Double-prepend would produce two marker lines and break comment detection. Tests verify the Python side only.**

Write the following failing tests (they will fail because the functions don't exist yet in the helper — Plan 02 adds them):

**Group 1 — CI-01: marker in comment bodies**
- `test_build_comment_includes_marker`: call `gdc.build_comment(...)` with dummy args, assert the returned string starts with `<!-- acd-diff-report -->` as its very first line.
- `test_build_no_files_comment_includes_marker`: call `gdc.build_no_files_comment("abc1234", "2026-03-15 12:00 UTC")`, assert the returned string starts with `<!-- acd-diff-report -->`.

**Group 2 — CI-02: is_private_repo**
- `test_is_private_repo_returns_true_on_missing_env`: call `gdc.is_private_repo()` with no GITHUB_TOKEN/GITHUB_REPOSITORY env vars set — assert returns True (conservative default).
- `test_is_private_repo_returns_false_for_public`: mock `urllib.request.urlopen` to return a JSON response `{"private": False}` — assert `gdc.is_private_repo()` returns False.
- `test_is_private_repo_returns_true_on_exception`: mock `urllib.request.urlopen` to raise an exception — assert `gdc.is_private_repo()` returns True.

**Group 3 — CI-02: per-file report table**
- `test_build_comment_contains_report_table_when_html_exists`: call `gdc.build_comment(sections=[], files=["Workflow.yxmd"], html_count=1, short_sha="abc1234", timestamp="2026-03-15", totals={"added":0,"removed":0,"modified":0}, errors=0, run_url="https://github.com/owner/repo/actions/runs/123")` — assert the returned comment body contains `| Workflow File |` (the per-file table header). Note the signature change: `run_url` is passed explicitly rather than fetched from env — Plan 02 updates the function signature.
- `test_build_comment_has_no_table_when_no_html`: call with `html_count=0` — assert `| Workflow File |` is NOT in the returned string.

These tests MUST fail when run right now (RED). The failures confirm the scaffold is correct. Expected failure: `AttributeError: module 'generate_diff_comment' has no attribute 'is_private_repo'` and similar for the per-file table.

Do NOT write implementations — only test code.
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && python -m pytest tests/test_ci_github_comment.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>tests/test_ci_github_comment.py exists; running pytest on it produces failures (not collection errors) — at minimum AttributeError on missing is_private_repo or assertion failures on missing marker. Zero collection errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove test-job placeholder from GitLab CI (CI-03)</name>
  <files>/Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml</files>
  <action>
**PREFLIGHT: Before making any edits, verify the file exists:**
```bash
test -f /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml && echo "EXISTS" || echo "NOT FOUND — stop and report path error"
```
If NOT FOUND, stop immediately and report the error. Do not attempt to create or edit the file at an alternative path.

Edit /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml:

1. Remove the entire `test-job` block (the job with `stage: test` and `script: - echo "Pipeline triggered successfully."`).
2. Remove `- test` from the `stages:` list, leaving only `- diff`.

The result should be:
```yaml
stages:
  - diff
```

And the `test-job:` block (approximately lines 23-26) must not exist. The `alteryx-diff` job and everything below it must remain unchanged.

No other changes.
  </action>
  <verify>
    <automated>grep -c "test-job" /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml</automated>
  </verify>
  <done>grep -c "test-job" returns 0. The stages: list contains only "- diff". The alteryx-diff job and all its configuration is intact.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ci_github_comment.py -q` → tests FAIL with AttributeError (not collection error) — RED confirmed
- `grep -c "test-job" /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml` → returns 0
- `grep "stages" /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml` → shows only `- diff`
</verification>

<success_criteria>
- tests/test_ci_github_comment.py exists with 7+ test functions; pytest collection succeeds; all tests fail with AttributeError or AssertionError (RED state)
- /Users/laxmikantmukkawar/alteryx/.gitlab-ci.yml has no test-job block, stages list contains only diff (CI-03 done)
</success_criteria>

<output>
After completion, create `.planning/phases/18-ci-polish/18-01-SUMMARY.md`
</output>
