---
phase: 17-branch-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_branch.py
  - app/routers/branch.py
  - app/server.py
autonomous: true
requirements:
  - BRANCH-01
  - BRANCH-02
  - BRANCH-03
must_haves:
  truths:
    - "pytest tests/test_branch.py -x exits non-zero (all RED) after this plan"
    - "GET /api/branch/{project_id} route exists and returns 404/422 (stub registered)"
    - "POST /api/branch/{project_id}/create route stub exists"
    - "POST /api/branch/{project_id}/checkout route stub exists"
    - "DELETE /api/branch/{project_id}/delete route stub exists"
  artifacts:
    - path: "tests/test_branch.py"
      provides: "RED test suite for all BRANCH-01/02/03 requirements"
      min_lines: 80
    - path: "app/routers/branch.py"
      provides: "APIRouter stub with 4 endpoints — implementations raise NotImplementedError"
      exports: ["router"]
    - path: "app/server.py"
      provides: "branch.router registered via app.include_router"
      contains: "branch"
  key_links:
    - from: "app/server.py"
      to: "app/routers/branch.py"
      via: "app.include_router(branch.router)"
      pattern: "include_router.*branch"
    - from: "tests/test_branch.py"
      to: "app/server.py"
      via: "TestClient(app)"
      pattern: "TestClient"
---

<objective>
Wave 0: create the test scaffold for Phase 17 branch management. All tests must be RED (failing) after this plan because the git_ops functions and endpoint implementations are stubs.

Purpose: Establish the test contracts before writing production code — RED-GREEN discipline for BRANCH-01/02/03.
Output: tests/test_branch.py (all RED), app/routers/branch.py (stub), server.py (router registered).
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-branch-management/17-CONTEXT.md
@.planning/phases/17-branch-management/17-RESEARCH.md

<interfaces>
<!-- From app/server.py — existing router registration pattern -->
```python
from app.routers import (
    projects, git_identity, folder_picker, watch, save, history, settings, remote
)
app.include_router(projects.router)
app.include_router(remote.router)  # last registered currently
```

<!-- From app/routers/history.py — module-level import pattern for mock.patch -->
```python
from app.services import git_ops  # noqa: F401 — required for mock.patch targeting
router = APIRouter(prefix="/api/history", tags=["history"])
```

<!-- From tests/ pattern — TestClient usage -->
```python
from fastapi.testclient import TestClient
from app.server import app
client = TestClient(app)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for all BRANCH requirements</name>
  <files>tests/test_branch.py</files>
  <behavior>
    - test_git_list_branches: mock git_ops.git_list_branches → assert GET /api/branch/{id}?folder=... returns [{name, is_current}]
    - test_git_create_branch: mock git_ops.git_create_branch → assert POST /api/branch/{id}/create with {folder, description} returns {branch_name: "experiment/...", success: True}
    - test_branch_name_format: POST /api/branch/{id}/create with description="price calc test" → branch_name starts with "experiment/" and contains "price-calc-test"
    - test_git_checkout: mock git_ops.git_checkout + git_changed_workflows → assert POST /api/branch/{id}/checkout with {folder, branch} returns {success: True}
    - test_checkout_blocked_if_dirty: mock git_changed_workflows to return ["file.yxmd"] → POST checkout returns {success: False, error contains "Save changes"}
    - test_git_delete_branch: mock git_ops.git_delete_branch → assert DELETE /api/branch/{id}/delete with {folder, branch} returns {success: True}
    - test_delete_main_blocked: DELETE with branch="main" → {success: False, error contains "Cannot delete"}
    - test_list_branches_endpoint: integration call, checks response is a list
    - test_list_history_with_branch: import TestClient, call GET /api/history/{id}?folder=...&branch=main → assert 200 (additive test in test_branch.py, not test_history.py — avoids editing existing test file)
    - test_get_merge_base_endpoint: mock subprocess.run → assert GET /api/branch/{id}/merge-base?folder=...&branch=experiment/2026-03-15-test returns {"merge_base_sha": "abc123"}; when both main and master fail → returns {"merge_base_sha": null}
  </behavior>
  <action>
    Create tests/test_branch.py. Import TestClient, app, unittest.mock.patch.

    Use the established pattern from test_remote.py and test_save.py:
    - Module-level client = TestClient(app)
    - Patch targets: "app.routers.branch.git_ops" for all git_ops mocks
    - Patch target for dirty check: "app.routers.branch.git_ops.git_changed_workflows"
    - For test_get_merge_base_endpoint, patch "app.routers.branch.subprocess.run"

    All tests MUST FAIL at this point because branch.py stubs raise NotImplementedError.

    Run after writing: python -m pytest tests/test_branch.py -x -q
    Expected: all tests FAIL with NotImplementedError or AttributeError (RED state confirmed).
    If any test passes, it is incorrectly written — fix it to actually test the behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_branch.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_branch.py exists, python -m pytest tests/test_branch.py runs and shows failures (RED). No collection errors (ImportError / SyntaxError).</done>
</task>

<task type="auto">
  <name>Task 2: Create branch.py stub router + register in server.py</name>
  <files>app/routers/branch.py, app/server.py</files>
  <action>
    Create app/routers/branch.py:
    - Import: `from app.services import git_ops  # noqa: F401 — required for mock.patch targeting`
    - Import: `from fastapi import APIRouter, Query`, `from pydantic import BaseModel`
    - Router: `router = APIRouter(prefix="/api/branch", tags=["branch"])`
    - Define request models: `BranchCreateRequest(folder: str, description: str)`, `BranchCheckoutRequest(folder: str, branch: str)`, `BranchDeleteRequest(folder: str, branch: str, force: bool = False)`
    - Endpoints (all raise NotImplementedError — stubs only):
      - `GET /{project_id}` → `raise NotImplementedError`
      - `POST /{project_id}/create` → `raise NotImplementedError`
      - `POST /{project_id}/checkout` → `raise NotImplementedError`
      - `DELETE /{project_id}/delete` → `raise NotImplementedError`
      - `GET /{project_id}/merge-base` → `raise NotImplementedError`

    IMPORTANT: Branch name params (like `experiment/2026-03-15-test`) contain `/` which breaks URL path segments.
    All branch name inputs MUST be in request body (POST/DELETE) or query params — never in path segments. The `project_id` path param only identifies the project.

    Update app/server.py:
    - Add `branch` to the existing router import block: `from app.routers import (..., branch)`
    - Add `app.include_router(branch.router)` after `app.include_router(remote.router)`

    Run: python -m pytest tests/test_branch.py -x -q
    Expected: same RED failures as Task 1 (stubs now exist and are registered, tests still fail because logic is not implemented).
  </action>
  <verify>
    <automated>python -m pytest tests/test_branch.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>app/routers/branch.py exists with 5 stub endpoints (including merge-base). app/server.py imports and registers branch.router. Tests still RED (NotImplementedError). Full suite import check: python -m pytest tests/ --collect-only -q exits 0.</done>
</task>

</tasks>

<verification>
python -m pytest tests/ --collect-only -q  (no collection errors)
python -m pytest tests/test_branch.py -x -q  (all RED — expected at end of Plan 01)
python -m pytest tests/ -x -q --ignore=tests/test_branch.py  (existing suite still GREEN)
</verification>

<success_criteria>
- tests/test_branch.py collected without errors, all tests fail (RED)
- app/routers/branch.py has stub endpoints for GET list, POST create, POST checkout, DELETE delete, GET merge-base
- app/server.py registers branch.router
- Existing test suite (excluding test_branch.py) remains GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/17-branch-management/17-01-SUMMARY.md`
</output>
