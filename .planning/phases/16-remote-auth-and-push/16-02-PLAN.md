---
phase: 16-remote-auth-and-push
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - app/services/remote_auth.py
  - app/services/github_api.py
  - app/services/gitlab_api.py
autonomous: true
requirements:
  - REMOTE-01
  - REMOTE-02
  - REMOTE-03

must_haves:
  truths:
    - "GitHub device flow start returns user_code and verification_uri from GitHub's API"
    - "GitHub device flow polling handles authorization_pending (continue), slow_down (+5s interval), expired_token and access_denied (abort)"
    - "GitHub and GitLab tokens are stored in and retrieved from OS keyring — never in plaintext or config_store"
    - "GitLab PAT is validated via GET /api/v4/user and stored in keyring on success"
    - "GitHub repo creation calls POST /user/repos with private:true and returns clone_url"
    - "Name collision detection appends -2, -3 suffix until an available name is found"
    - "All tests in tests/test_remote.py for REMOTE-01, REMOTE-02, REMOTE-03 pass GREEN"
  artifacts:
    - path: "app/services/remote_auth.py"
      provides: "request_device_code, poll_and_store (asyncio), get/store github/gitlab tokens via keyring, _ensure_backend() PyInstaller fix"
      exports:
        - request_device_code
        - poll_and_store
        - get_github_token
        - store_github_token
        - get_gitlab_token
        - store_gitlab_token
        - validate_and_store_gitlab_token
    - path: "app/services/github_api.py"
      provides: "create_github_repo, get_github_username, github_repo_exists, find_available_repo_name, slugify_folder_name"
      exports:
        - create_github_repo
        - get_github_username
        - github_repo_exists
        - find_available_repo_name
    - path: "app/services/gitlab_api.py"
      provides: "validate_gitlab_token, create_gitlab_project"
      exports:
        - validate_gitlab_token
        - create_gitlab_project
  key_links:
    - from: "app/services/remote_auth.py"
      to: "keyring"
      via: "_ensure_backend() + keyring.set_password / get_password"
      pattern: "keyring\\.set_password|keyring\\.get_password"
    - from: "app/services/remote_auth.py"
      to: "https://github.com/login/device/code"
      via: "httpx.post"
      pattern: "httpx\\.post.*device/code"
    - from: "app/services/github_api.py"
      to: "https://api.github.com/user/repos"
      via: "httpx.post"
      pattern: "httpx\\.post.*user/repos"
---

<objective>
Implement the three auth/credential service files: remote_auth.py (GitHub device flow + keyring), github_api.py (REST repo operations), gitlab_api.py (PAT validation + repo creation). Turn REMOTE-01, REMOTE-02, REMOTE-03 tests GREEN.

Purpose: All credential handling and remote service API calls live in dedicated service modules, following module-level import pattern for mock.patch compatibility.
Output: 3 production service files, tests GREEN for REMOTE-01 through REMOTE-03.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-remote-auth-and-push/16-RESEARCH.md
@.planning/phases/16-remote-auth-and-push/16-01-SUMMARY.md

<interfaces>
<!-- Key implementation patterns from research -->

GitHub Device Flow endpoints:
- POST https://github.com/login/device/code → {device_code, user_code, verification_uri, expires_in, interval}
- POST https://github.com/login/oauth/access_token → {access_token} | {error: "authorization_pending"|"slow_down"|"expired_token"|"access_denied"}
- Polling error codes: authorization_pending=continue, slow_down=interval+=5 then continue, expired_token=abort, access_denied=abort

keyring API:
- keyring.set_password(service, username, password)
- keyring.get_password(service, username) -> str | None
- SERVICE_GITHUB = "AlteryxGitCompanion:github"
- SERVICE_GITLAB = "AlteryxGitCompanion:gitlab"
- USERNAME_KEY = "token"

PyInstaller fix (_ensure_backend):
```python
if getattr(sys, "frozen", False):
    if sys.platform == "win32":
        import keyring.backends.Windows
        keyring.set_keyring(keyring.backends.Windows.WinVaultKeyring())
    elif sys.platform == "darwin":
        import keyring.backends.macOS
        keyring.set_keyring(keyring.backends.macOS.Keyring())
```

GitHub REST:
- POST https://api.github.com/user/repos body: {"name": slug, "private": True, "auto_init": False}
- GET  https://api.github.com/user → {"login": username}
- GET  https://api.github.com/repos/{owner}/{name} → 200 if exists, 404 if not
- Headers: Authorization: Bearer {token}, Accept: application/vnd.github+json, X-GitHub-Api-Version: 2022-11-28

GitLab REST (gitlab.com):
- GET  https://gitlab.com/api/v4/user headers: PRIVATE-TOKEN: {token} → 200 + {username} or 401
- POST https://gitlab.com/api/v4/projects body: {"name": slug, "visibility": "private"}
  headers: PRIVATE-TOKEN: {token}
- .http_url_to_repo is the clone URL

Slug generation (no external dep):
```python
import re
def slugify_folder_name(name: str) -> str:
    slug = re.sub(r'[^a-z0-9]+', '-', name.lower())
    return slug.strip('-') or 'my-workflows'
```

poll_and_store must be an async def (called via asyncio.create_task from router):
```python
async def poll_and_store(device_code: str, interval: int) -> None:
    deadline = asyncio.get_event_loop().time() + 900
    while asyncio.get_event_loop().time() < deadline:
        await asyncio.sleep(interval)
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement remote_auth.py — device flow + keyring</name>
  <files>app/services/remote_auth.py</files>
  <behavior>
    - test_request_device_code: calls request_device_code(), gets dict with 'user_code' and 'device_code'
    - test_poll_authorization_pending: authorization_pending loops, access_token terminates and returns token
    - test_poll_slow_down_increases_interval: slow_down causes interval+5 on next sleep call
    - test_store_and_get_github_token: store_github_token("tok") calls keyring with SERVICE_GITHUB; get_github_token() returns same
    - test_credentials_not_in_config_store: store_github_token does NOT call config_store.save_config
    - test_validate_gitlab_token_valid: validate returns user dict on 200
    - test_validate_gitlab_token_invalid: validate returns None on non-200
  </behavior>
  <action>
    Replace the stub in app/services/remote_auth.py with full implementation.

    Constants:
    - DEVICE_CODE_URL = "https://github.com/login/device/code"
    - TOKEN_URL = "https://github.com/login/oauth/access_token"
    - GITLAB_BASE = "https://gitlab.com/api/v4"
    - CLIENT_ID = "YOUR_GITHUB_CLIENT_ID"  # TODO: replace with registered client_id before ship
    - SERVICE_GITHUB = "AlteryxGitCompanion:github"
    - SERVICE_GITLAB = "AlteryxGitCompanion:gitlab"
    - USERNAME_KEY = "token"

    Functions to implement:
    1. _ensure_backend() — PyInstaller fix, called at module import time
    2. request_device_code() -> dict — httpx.post to DEVICE_CODE_URL, Accept: application/json
    3. async poll_and_store(device_code, interval) -> None — asyncio polling loop; handles authorization_pending (continue), slow_down (interval += 5), expired_token/access_denied (return); on success calls store_github_token(token)
    4. store_github_token(token) / get_github_token() — keyring.set_password/get_password using SERVICE_GITHUB + USERNAME_KEY
    5. store_gitlab_token(token) / get_gitlab_token() — same for SERVICE_GITLAB
    6. validate_gitlab_token(token) -> dict | None — httpx.get to GITLAB_BASE/user with PRIVATE-TOKEN header; returns json on 200, None otherwise
    7. validate_and_store_gitlab_token(token) -> bool — calls validate_gitlab_token, stores if valid, returns True/False

    Use httpx (not requests) for all HTTP calls.
    Module-level _ensure_backend() call at bottom of imports (before any function definitions).
    poll_and_store uses asyncio.sleep (not time.sleep) — it is a coroutine called via asyncio.create_task.
  </action>
  <verify>
    <automated>uv run pytest tests/test_remote.py -k "device_code or slow_down or pending or token or gitlab_token or credentials" -x -q</automated>
  </verify>
  <done>All REMOTE-01, REMOTE-02, REMOTE-03 unit tests pass GREEN; no test imports fail</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement github_api.py and gitlab_api.py — REST repo operations</name>
  <files>app/services/github_api.py, app/services/gitlab_api.py</files>
  <behavior>
    - test_create_github_repo_private: mock httpx.post, create_github_repo("tok", "my-workflows") POSTs to /user/repos with {"private": True}; returns response dict with clone_url
    - test_find_available_repo_name_collision: github_repo_exists returns [True, True, False]; find_available_repo_name returns "repo-3"
    - test_create_gitlab_project_private: mock httpx.post, create_gitlab_project("tok", "my-workflows") POSTs to /projects with {"visibility": "private"}
  </behavior>
  <action>
    Replace stubs in github_api.py and gitlab_api.py with full implementations.

    github_api.py functions:
    1. get_github_username(token: str) -> str — GET https://api.github.com/user, returns json["login"]
    2. github_repo_exists(token: str, owner: str, repo_name: str) -> bool — GET https://api.github.com/repos/{owner}/{repo_name}, returns status_code == 200
    3. create_github_repo(token: str, name: str) -> dict — POST https://api.github.com/user/repos, body {"name": name, "private": True, "auto_init": False}, returns response json (.clone_url is HTTPS remote URL)
    4. find_available_repo_name(token: str, owner: str, base_slug: str) -> str — loop: candidate=base_slug, if exists append -2, -3, etc; return first non-colliding name
    5. slugify_folder_name(name: str) -> str — re.sub(r'[^a-z0-9]+', '-', name.lower()).strip('-') or 'my-workflows'

    All GitHub API calls use headers: {"Authorization": f"Bearer {token}", "Accept": "application/vnd.github+json", "X-GitHub-Api-Version": "2022-11-28"}

    gitlab_api.py functions:
    1. GITLAB_BASE = "https://gitlab.com/api/v4"
    2. validate_gitlab_token(token: str) -> dict | None — GET GITLAB_BASE/user, PRIVATE-TOKEN header; return json or None
    3. create_gitlab_project(token: str, name: str) -> dict — POST GITLAB_BASE/projects, {"name": name, "visibility": "private"}, returns json (.http_url_to_repo is clone URL)

    Both modules use httpx for all HTTP calls. Module-level imports only — no lazy imports — so mock.patch works correctly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_remote.py -k "github_repo or gitlab_project or available_repo" -x -q</automated>
  </verify>
  <done>REMOTE-05 unit tests pass GREEN; all 3 test functions listed in behavior pass; uv run pytest tests/test_remote.py -x passes all REMOTE-01 through REMOTE-03 tests</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_remote.py -x -q
All tests covering REMOTE-01, REMOTE-02, REMOTE-03 and REMOTE-05 unit tests pass GREEN.
uv run pytest tests/ -x — full suite still green (no regressions).
</verification>

<success_criteria>
- uv run pytest tests/test_remote.py -x exits 0 for all REMOTE-01/02/03 and REMOTE-05 unit tests
- keyring service names are "AlteryxGitCompanion:github" and "AlteryxGitCompanion:gitlab"
- _ensure_backend() exists in remote_auth.py and is called at module import time
- No credentials flow through config_store
- CLIENT_ID constant present in remote_auth.py with TODO comment for registration
</success_criteria>

<output>
After completion, create `.planning/phases/16-remote-auth-and-push/16-02-SUMMARY.md`
</output>
