---
phase: 16-remote-auth-and-push
plan: 05
type: execute
wave: 4
depends_on:
  - 16-04
files_modified: []
autonomous: false
requirements:
  - REMOTE-01
  - REMOTE-02
  - REMOTE-03
  - REMOTE-04
  - REMOTE-05
  - REMOTE-06

must_haves:
  truths:
    - "User can open the Remote panel by clicking the cloud icon in the sidebar"
    - "GitHub device flow shows a user_code inline with Copy and Open-browser buttons"
    - "After completing GitHub OAuth in the browser, the app shows green Connected status without requiring a restart"
    - "GitLab PAT instructions are numbered 1-2-3 and include a working Open GitLab Settings link"
    - "After saving a version, the Push button sends it to GitHub or GitLab and ahead/behind count updates"
    - "Auth credentials survive an app restart (OS keyring persists)"
    - "On first push, app auto-creates a private repo and shows the repo name in the confirmation copy"
  artifacts:
    - path: "app/frontend/src/components/RemotePanel.tsx"
      provides: "Full UI verified working end-to-end by human"
  key_links: []
---

<objective>
Human verification checkpoint for Phase 16. All REMOTE-01 through REMOTE-06 requirements verified end-to-end by the user with real GitHub/GitLab accounts.

Purpose: Backend tests cover the logic, but browser OAuth flows, OS keychain persistence, and actual git push to a real remote require human sign-off.
Output: Human confirms all 6 REMOTE requirements working. Phase 16 complete.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-remote-auth-and-push/16-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:pre-verify" gate="blocking">
  <what-to-check>Confirm automated baseline is green before human sign-off</what-to-check>
  <how-to-verify>
    Run the full test suite:
    ```bash
    uv run pytest tests/ -x -q
    ```
    Build the frontend:
    ```bash
    cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff/app/frontend && npm run build
    ```
    Start the development server:
    ```bash
    cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && uv run uvicorn app.server:app --port 7433 --reload
    ```
    All three commands must succeed before proceeding to human verification.
  </how-to-verify>
  <resume-signal>Type "ready" once tests are green, build passes, and dev server is running on port 7433</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 16 — Remote Auth and Push:
    - Sidebar cloud icon that opens the Remote panel
    - GitHub Device Flow: shows user_code inline with Copy/Open-browser buttons, polls until authorized, shows green Connected badge
    - GitLab PAT: numbered 1-2-3 instructions with Open GitLab Settings link, PAT input field, connect button
    - Ahead/behind indicator above Push button (↑ N ahead · ↓ N behind)
    - Push button that creates repo on first push and shows inline confirmation copy
    - Credentials persisted in OS keyring — survive restart
  </what-built>
  <how-to-verify>
    Open http://localhost:7433 in your browser. With a registered project folder open:

    **REMOTE-01 — GitHub Device Flow:**
    1. Click the cloud icon in the sidebar. Remote panel should open.
    2. Click the GitHub tab. Click [Connect GitHub].
    3. A user code appears inline. Click [Copy Code] — code is copied to clipboard.
    4. Click [Open github.com/login/device]. Browser opens GitHub device auth page.
    5. Paste the code, click Authorize. Return to the app.
    6. Within 30 seconds, the app should show a green "Connected" badge on the GitHub tab.

    **REMOTE-02 — GitLab PAT:**
    7. Click the GitLab tab. Confirm numbered 1-2-3 instructions are shown.
    8. Click [Open GitLab Settings]. Confirm it opens the GitLab personal access token page.
    9. (Optional) Generate a PAT with "api" scope, paste it, click [Connect GitLab]. Confirm green Connected badge.

    **REMOTE-03 — Credential persistence:**
    10. Restart the app (kill and relaunch uvicorn). Open the Remote panel.
    11. Confirm GitHub still shows "Connected" without re-authenticating.

    **REMOTE-04 + REMOTE-05 — Push and auto-create repo:**
    12. Ensure a version has been saved (use ChangesPanel to save one if needed).
    13. In the Remote panel, GitHub tab, confirm the push area shows: "No remote repo yet. We'll create **[folder-slug]** (private) on GitHub."
    14. Click [Push and Create Repo]. After a few seconds: success state shown.
    15. Visit GitHub.com — confirm a new private repo exists with the expected name.
    16. Push again (make another save first). Confirm push succeeds without re-creating the repo.

    **REMOTE-06 — Ahead/behind indicator:**
    17. After push, confirm "↑ 0 ahead · ↓ 0 behind" (all synced).
    18. Save another version locally (don't push). Re-open the Remote panel.
    19. Confirm "↑ 1 ahead · ↓ 0 behind".
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass, or describe any issues found (e.g., "GitHub tab not showing user_code", "push failed with error message X").
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 6 REMOTE requirements. All automated tests green confirmed in Task 1.
</verification>

<success_criteria>
- uv run pytest tests/ -x exits 0 (Task 1 pre-verify)
- Human verified all 6 REMOTE requirements working end-to-end (Task 2)
- GitHub OAuth device flow completes in-app without manual URL entry
- GitLab PAT instructions clear and link works
- Credentials survive restart
- Push creates repo on first use and succeeds on subsequent pushes
- Ahead/behind indicator accurate after push
</success_criteria>

<output>
After completion, create `.planning/phases/16-remote-auth-and-push/16-05-SUMMARY.md`
</output>
