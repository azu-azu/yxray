---
phase: 10-app-scaffold
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - app.spec
  - version_info.yml
  - assets/icon.ico
  - .github/workflows/release.yml
  - .gitignore
autonomous: true
requirements:
  - APP-01
  - APP-03
  - APP-04a

must_haves:
  truths:
    - "PyInstaller builds the .exe without errors when run via make package on a machine with the frontend dist/ built"
    - "The .spec file includes bootloader_ignore_signals=True to prevent double-SIGINT shutdown crash"
    - "The .spec file declares all uvicorn hidden imports so the exe does not crash with ModuleNotFoundError at runtime"
    - "The .spec bundles app/frontend/dist and src/alteryx_diff/static into the exe"
    - "acd CLI package is bundled and importable from within the exe at runtime (alteryx_diff.pipeline.pipeline is included in the bundle via Analysis pathex/datas)"
    - "version_info.yml defines FileDescription, ProductName, and OriginalFilename for Windows VERSIONINFO"
    - "GitHub Actions release.yml triggers on v* tag push, builds on windows-latest, and uploads exe to GitHub Releases"
    - "console=True in the spec for Phase 10 scaffold (Phase 15 will flip this to False)"
  artifacts:
    - path: "app.spec"
      provides: "PyInstaller build spec with datas, hiddenimports, icon, version, bootloader settings"
      contains: "bootloader_ignore_signals"
    - path: "version_info.yml"
      provides: "Windows VERSIONINFO source for pyinstaller-versionfile"
      contains: "FileDescription"
    - path: "assets/icon.ico"
      provides: "Placeholder .ico icon (16x16 + 32x32) for the exe"
    - path: ".github/workflows/release.yml"
      provides: "CI workflow: tag push to Windows build to GitHub Release"
      contains: "softprops/action-gh-release"
  key_links:
    - from: "app.spec"
      to: "app/main.py"
      via: "Analysis(['app/main.py'], ...)"
      pattern: "app/main\\.py"
    - from: "app.spec"
      to: "app/frontend/dist"
      via: "datas=[('app/frontend/dist', 'frontend/dist'), ...]"
      pattern: "frontend/dist"
    - from: "app.spec"
      to: "src/alteryx_diff"
      via: "pathex or datas entry ensuring alteryx_diff package is discovered by Analysis"
      pattern: "alteryx_diff"
    - from: ".github/workflows/release.yml"
      to: "app.spec"
      via: "uv run pyinstaller app.spec"
      pattern: "pyinstaller app\\.spec"
---

<objective>
Create the PyInstaller spec file, Windows VERSIONINFO, placeholder icon, and GitHub Actions CI workflow that produces the distributable .exe automatically on tag push.

Purpose: This is the packaging layer that wires together the backend (Plan 01) and compiled frontend (Plan 02) into a single self-contained Windows .exe. The GitHub Actions workflow eliminates any need for a local Windows dev machine to produce releases.

Output: app.spec, version_info.yml, assets/icon.ico (placeholder), .github/workflows/release.yml. Running make package on a machine with dist/ built produces dist/AlterxyGitCompanion.exe.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-app-scaffold/10-CONTEXT.md
@.planning/phases/10-app-scaffold/10-RESEARCH.md
@.planning/phases/10-app-scaffold/10-01-SUMMARY.md
@.planning/phases/10-app-scaffold/10-02-SUMMARY.md
</context>

<interfaces>
From app/server.py (created in Plan 01):
  _static_dir() returns Path(sys._MEIPASS) / "frontend" / "dist" when frozen.
  Therefore app.spec datas entry must be: ('app/frontend/dist', 'frontend/dist')

From src/alteryx_diff/static/:
  Contains vis-network.min.js and other acd CLI HTML report assets.
  Spec datas entry: ('src/alteryx_diff/static', 'alteryx_diff/static')

From app/main.py (created in Plan 01):
  Entry point for PyInstaller Analysis: ['app/main.py']

From src/alteryx_diff/ (existing acd CLI package):
  The acd CLI is bundled as a dependency inside the exe; FastAPI backend calls it via pipeline.run().
  Ensure src/ is in Analysis pathex so PyInstaller discovers alteryx_diff as a package:
  pathex=['src']
  This makes `from alteryx_diff.pipeline.pipeline import run` work inside the bundled exe.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create PyInstaller spec, version_info.yml, and placeholder icon</name>
  <files>app.spec, version_info.yml, assets/icon.ico, .gitignore</files>
  <action>
    Create assets/ directory if it does not exist.

    Step 1 — Create assets/icon.ico placeholder using Python (no external tools required):
    Run this Python script to generate a minimal valid .ico file:

      python -c "
      import struct, pathlib
      pathlib.Path('assets').mkdir(exist_ok=True)
      w, h = 16, 16
      bmp_header = struct.pack(
          '<IiiHHIIiiII',
          40, w, h*2, 1, 4, 0,
          w * h // 2, 0, 0, 16, 16
      )
      palette = bytes([0x1e,0x3a,0x5f,0]*16)
      xor_data = bytes([0x00]*(w*h//2))
      and_data = bytes([0x00]*(w*h//8))
      img = bmp_header + palette + xor_data + and_data
      ico = struct.pack('<HHH',0,1,1) + struct.pack('<BBBBHHII',16,16,16,0,1,4,len(img),22) + img
      pathlib.Path('assets/icon.ico').write_bytes(ico)
      print('assets/icon.ico created:', len(ico), 'bytes')
      "

    Step 2 — Create version_info.yml:

      Version: "1.0.0.0"
      CompanyName: ""
      FileDescription: "Alteryx Git Companion"
      InternalName: "AlterxyGitCompanion"
      LegalCopyright: ""
      OriginalFilename: "AlterxyGitCompanion.exe"
      ProductName: "Alteryx Git Companion"

    Step 3 — Create app.spec with these exact settings:
    - entry point: app/main.py
    - pathex: ['src'] — ensures PyInstaller's Analysis finds the alteryx_diff package (src/alteryx_diff/) so it is bundled into the exe and importable at runtime
    - datas: ('app/frontend/dist', 'frontend/dist') and ('src/alteryx_diff/static', 'alteryx_diff/static')
    - hiddenimports: all 12 uvicorn submodules listed in RESEARCH.md (uvicorn.logging, uvicorn.loops, uvicorn.loops.auto, uvicorn.loops.asyncio, uvicorn.protocols, uvicorn.protocols.http, uvicorn.protocols.http.auto, uvicorn.protocols.http.h11_impl, uvicorn.protocols.websockets, uvicorn.protocols.websockets.auto, uvicorn.lifespan, uvicorn.lifespan.on)
    - excludes: ['tkinter', 'matplotlib', 'PIL']
    - EXE settings: name='AlterxyGitCompanion', bootloader_ignore_signals=True, upx=False, console=True (Phase 10 debug mode), icon='assets/icon.ico', version='file_version_info.txt'
    - block_cipher = None
    - Standard PyInstaller onefile pattern: Analysis -> PYZ -> EXE (all in one file, no COLLECT step)

    Step 4 — Update root .gitignore to exclude build artifacts.
    Append these lines if not already present:
      dist/
      build/
      file_version_info.txt
    Note: app/frontend/dist is handled by app/frontend/.gitignore — do not add it here.

    Step 5 — Verify spec is valid Python syntax:
    python -c "
    src = open('app.spec').read()
    import ast
    try:
        ast.parse(src)
    except SyntaxError as e:
        print('SYNTAX ERROR:', e)
        raise
    print('app.spec: syntax OK')
    "
  </action>
  <verify>
    <automated>python -c "
import ast, sys, yaml, pathlib
errors = []

src = open('app.spec').read()
try:
    ast.parse(src)
except SyntaxError as e:
    errors.append(f'app.spec SyntaxError: {e}')

if 'bootloader_ignore_signals=True' not in src:
    errors.append('app.spec missing bootloader_ignore_signals=True')
if 'console=True' not in src:
    errors.append('app.spec missing console=True')
if 'upx=False' not in src:
    errors.append('app.spec missing upx=False')
if \"('app/frontend/dist', 'frontend/dist')\" not in src:
    errors.append('app.spec missing frontend/dist datas entry')
if \"'src'\" not in src and '\"src\"' not in src:
    errors.append('app.spec missing pathex src entry (needed for alteryx_diff bundle)')

vi = yaml.safe_load(open('version_info.yml'))
if vi.get('FileDescription') != 'Alteryx Git Companion':
    errors.append('version_info.yml FileDescription wrong')

ico = pathlib.Path('assets/icon.ico').read_bytes()
if ico[2:4] != b'\\x01\\x00':
    errors.append('assets/icon.ico invalid ICO header')

if errors:
    print('FAILURES:')
    for e in errors: print(' -', e)
    sys.exit(1)
print('All checks passed')
"</automated>
  </verify>
  <done>app.spec is valid Python with bootloader_ignore_signals=True, console=True, upx=False, both datas entries, pathex=['src'] for acd CLI bundling, and all 12 uvicorn hiddenimports. version_info.yml has correct FileDescription and ProductName. assets/icon.ico is a valid .ico binary.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions release workflow</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Create .github/workflows/ directory if it does not exist.

    Create .github/workflows/release.yml with the following structure:
    - Trigger: push of tags matching 'v*' pattern
    - Job name: build-windows
    - Runner: windows-latest
    - Steps in order:
      1. actions/checkout@v4
      2. actions/setup-python@v5 with python-version: '3.11' (no pip cache — using uv)
      3. pip install uv
      4. uv sync (installs all dependencies including pyinstaller, fastapi, uvicorn)
      5. actions/setup-node@v4 with node-version: '20', cache: 'npm', cache-dependency-path: app/frontend/package-lock.json
      6. Build frontend: working-directory: app/frontend, run: npm ci && npm run build
      7. Generate VERSIONINFO: uv run pyivf-make_version --source-format yaml --metadata-source version_info.yml --outfile file_version_info.txt --version ${{ github.ref_name }}
      8. Build exe: uv run pyinstaller app.spec --noconfirm
      9. Upload release: softprops/action-gh-release@v2 with files: dist/AlterxyGitCompanion.exe

    The release body must include Windows SmartScreen bypass instructions:
    "Windows may show a SmartScreen warning. Click More info then Run anyway."

    Use softprops/action-gh-release@v2 (NOT the deprecated actions/create-release@v1 or actions/upload-release-asset@v1).

    Verify YAML is valid after creation:
    python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml')); print('YAML valid')"
  </action>
  <verify>
    <automated>python -c "
import yaml, sys
data = yaml.safe_load(open('.github/workflows/release.yml'))
errors = []
if data.get('on', {}).get('push', {}).get('tags') != ['v*']:
    errors.append('trigger: push v* tag missing')
jobs = data.get('jobs', {})
job = list(jobs.values())[0] if jobs else {}
if job.get('runs-on') != 'windows-latest':
    errors.append('runs-on: windows-latest missing')
steps_str = str(job.get('steps', []))
if 'softprops/action-gh-release' not in steps_str:
    errors.append('softprops/action-gh-release missing')
if 'AlterxyGitCompanion.exe' not in steps_str:
    errors.append('exe filename missing from upload step')
if 'windows-latest' not in steps_str and job.get('runs-on') != 'windows-latest':
    errors.append('windows runner issue')
if errors:
    for e in errors: print('FAIL:', e)
    sys.exit(1)
print('release.yml: all checks passed')
"</automated>
  </verify>
  <done>.github/workflows/release.yml exists, is valid YAML, triggers on v* tag push, runs on windows-latest, builds React frontend, runs pyinstaller, and uploads AlterxyGitCompanion.exe to GitHub Release via softprops/action-gh-release@v2. Release body contains Windows Defender bypass instructions.</done>
</task>

</tasks>

<verification>
Full packaging layer verified:
- python -c "ast.parse(open('app.spec').read())" — no SyntaxError
- grep bootloader_ignore_signals app.spec — present and True
- grep "console=True" app.spec — present
- grep "upx=False" app.spec — present
- grep "frontend/dist" app.spec — appears in datas
- grep "alteryx_diff/static" app.spec — appears in datas
- grep "'src'" app.spec — appears in pathex (ensures acd CLI bundled)
- grep "softprops/action-gh-release" .github/workflows/release.yml — present
- ls assets/icon.ico — exists
- python -c "import yaml; yaml.safe_load(open('version_info.yml'))" — no error
</verification>

<success_criteria>
- app.spec is syntactically valid with bootloader_ignore_signals=True, console=True, upx=False
- app.spec datas bundles both app/frontend/dist and src/alteryx_diff/static
- app.spec pathex includes 'src' so alteryx_diff package is discovered and bundled
- app.spec hiddenimports lists all 12 uvicorn submodules
- version_info.yml has FileDescription and ProductName set to "Alteryx Git Companion"
- assets/icon.ico is a valid .ico binary file
- .github/workflows/release.yml triggers on v* tags, uses windows-latest, softprops/action-gh-release@v2
- Release body in workflow contains SmartScreen bypass instructions
- Root .gitignore excludes dist/ and file_version_info.txt
</success_criteria>

<output>
After completion, create `.planning/phases/10-app-scaffold/10-03-SUMMARY.md`
</output>
