---
phase: 12-file-watcher
plan: 05
type: execute
wave: 5
depends_on:
  - "12-04"
files_modified: []
autonomous: false
requirements:
  - WATCH-01
  - WATCH-02
  - WATCH-03
must_haves:
  truths:
    - "Amber badge appears in sidebar within 2 seconds of modifying a .yxmd file in a registered folder"
    - "Badge shows the correct count (e.g. '2' when 2 files are modified)"
    - "Badge disappears or updates after all changes are reverted (git checkout)"
    - "GET /api/watch/status returns has_any_commits: false for a folder with no commits"
  artifacts:
    - path: "app/services/watcher_manager.py"
      provides: "Confirmed live in running server"
    - path: "app/routers/watch.py"
      provides: "Confirmed live in running server"
  key_links:
    - from: "Running browser UI"
      to: "Sidebar amber badge"
      via: "SSE event received from /api/watch/events updates Zustand store"
      pattern: "changedCount"
---

<objective>
Human verification of the full Phase 12 file watcher loop: start the live server, register a project folder, modify a .yxmd file, confirm the amber badge appears in the sidebar within 2 seconds. Verify the /api/watch/status endpoint returns correct data.

Purpose: Automated tests validate logic and API contracts, but cannot verify OS-level file events, the SSE push reaching the browser, or the Tailwind badge rendering correctly in a live session.
Output: Human sign-off on all three WATCH requirements.
</objective>

<execution_context>
@/Users/laxmikantmukkawar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laxmikantmukkawar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-file-watcher/12-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Phase 12 file watcher implementation:
- WatcherManager singleton (watchdog observers, debounce, git status rescan)
- /api/watch/events SSE endpoint pushing badge_update events
- /api/watch/status endpoint with per-project changed_count, total_workflows, has_any_commits
- Zustand store setChangedCount action + useWatchEvents hook
- Amber badge in Sidebar (right-aligned, bg-amber-500, count number)
- projects.py add/remove notify watcher, server.py lifespan starts watcher on startup
  </what-built>
  <how-to-verify>
**Prerequisites: build the frontend and start the server**

```bash
# Terminal 1: build frontend
cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff/app/frontend
npm run build

# Terminal 2: start the server
cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff
python -m uvicorn app.server:app --port 7433 --reload
```

Open http://localhost:7433 in a browser.

---

**Test 1 — WATCH-01: Badge appears on file change**

1. Add a project folder that contains at least one .yxmd or .yxwz file AND has been git init-ed with at least one commit (so changes register as modifications vs HEAD). If needed, use the test fixtures folder or create one manually.
2. Open a second terminal and touch/modify a .yxmd file in that folder:
   ```bash
   echo " " >> /path/to/your/project/test.yxmd
   ```
3. Watch the sidebar in the browser — within 2 seconds, an amber badge with the count (e.g. "1") should appear on the right of the project row.
4. Modify a second file: badge should increment to "2".
5. Revert one file (`git checkout test.yxmd`): badge should drop back to "1" within 2 seconds.

**Expected:** Amber count badge appears and updates in near-real-time.

---

**Test 2 — WATCH-02: Observer selection (log check)**

In Terminal 2 (server logs), observe the startup log output. If the project folder is local:
- No "PollingObserver" mention in startup logs (native observer selected silently)

If you have access to a UNC or network path (optional):
- Register a \\server\share path and confirm server logs show PollingObserver selected

**Expected:** No manual configuration needed — observer selection is automatic.

---

**Test 3 — WATCH-03: watch/status endpoint**

```bash
curl http://localhost:7433/api/watch/status | python -m json.tool
```

**Expected response shape:**
```json
{
  "<project-id>": {
    "changed_count": N,
    "total_workflows": M,
    "has_any_commits": true/false
  }
}
```

For a folder that has been git-init-ed but has NO commits: `has_any_commits` must be `false`.
For a folder with at least one commit: `has_any_commits` must be `true`.
`total_workflows` must match the number of .yxmd/.yxwz files in the folder.

---

**Test 4 — Badge style check**

Confirm badge appearance:
- Color: amber/orange (not red, not green)
- Position: far right of project row (not left, not floating randomly)
- Text: plain number (no "+" or "!" prefix)
- Disappears when count returns to 0
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass. Describe any failures (which test, what you saw vs what was expected).</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 4 verification tests above.
All automated tests: python -m pytest tests/ -q (must be green before this checkpoint runs)
</verification>

<success_criteria>
- Test 1 (WATCH-01): Amber badge appears within 2 seconds of file modification, count is accurate, updates on additional changes
- Test 2 (WATCH-02): Observer selection is automatic (no manual config required)
- Test 3 (WATCH-03): /api/watch/status returns correct has_any_commits and total_workflows
- Test 4: Badge is amber, right-aligned, shows number, disappears at 0
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/12-file-watcher/12-05-SUMMARY.md`
</output>
