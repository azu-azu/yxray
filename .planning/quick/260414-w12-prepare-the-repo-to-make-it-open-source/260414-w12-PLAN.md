---
phase: 260414-w12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .gitignore
  - pyproject.toml
autonomous: true
requirements: [OSS-PREP]

must_haves:
  truths:
    - "Repository has an MIT LICENSE file at the root"
    - ".planning/, .claude/, .env, .env.*, tests/eval/results/, app/frontend/.vite/ are all gitignored"
    - "Previously tracked .planning/ files (291) are no longer in the git index"
    - "All .planning/ files still exist on disk after untracking"
    - "pyproject.toml declares license = MIT"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license for open-source publication"
      contains: "MIT License"
    - path: ".gitignore"
      provides: "Exclusion rules for sensitive/internal files"
      contains: ".planning/"
    - path: "pyproject.toml"
      provides: "Package metadata with license field"
      contains: "license"
  key_links:
    - from: ".gitignore"
      to: ".planning/"
      via: "gitignore rule prevents re-tracking after git rm --cached"
      pattern: "^\\.planning/"
---

<objective>
Prepare the alteryx_diff repository for open-source publication by adding an MIT license,
updating .gitignore to exclude sensitive/internal directories, untracking already-committed
planning files, and adding the license field to pyproject.toml.

Purpose: Make the repo safe and properly licensed for public visibility on GitHub.
Output: LICENSE file, updated .gitignore, updated pyproject.toml, 291 .planning/ files removed from git index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260414-w12-prepare-the-repo-to-make-it-open-source/260414-w12-CONTEXT.md
@.planning/quick/260414-w12-prepare-the-repo-to-make-it-open-source/260414-w12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MIT LICENSE and update .gitignore</name>
  <files>LICENSE, .gitignore</files>
  <action>
1. Create LICENSE file at repo root with the standard MIT License text.
   - Copyright year: 2026
   - Copyright holder: Laxmikant Mukkawar
   - Use the canonical text from choosealicense.com/licenses/mit/

2. Append the following block to the END of the existing .gitignore (after the existing entries):

```
# Open-source exclusions — sensitive/internal files
.env
.env.*
.claude/
.planning/
tests/eval/results/
app/frontend/.vite/
```

IMPORTANT: Do NOT modify or remove any existing .gitignore entries. Only append.
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && test -f LICENSE && grep -q "MIT License" LICENSE && grep -q ".planning/" .gitignore && grep -q ".env" .gitignore && grep -q ".claude/" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>LICENSE file exists with MIT text, copyright 2026 Laxmikant Mukkawar. .gitignore contains entries for .env, .env.*, .claude/, .planning/, tests/eval/results/, app/frontend/.vite/</done>
</task>

<task type="auto">
  <name>Task 2: Untrack .planning/ from git index and update pyproject.toml metadata</name>
  <files>pyproject.toml</files>
  <action>
1. Run `git rm --cached -r .planning/` to remove the 291 tracked .planning/ files from the git index.
   This does NOT delete local files -- they remain on disk. The .gitignore entry from Task 1 prevents
   them from being re-added.

2. Verify files still exist locally: `ls .planning/STATE.md` should succeed.

3. Update pyproject.toml to add the `license` field. Insert after the `readme` line:
   ```
   license = {text = "MIT"}
   ```
   Also add a `[project.urls]` section (after the `[project.scripts]` section or at end of [project] block):
   ```
   [project.urls]
   Repository = "https://github.com/LaxmikantM/alteryx_diff"
   Issues = "https://github.com/LaxmikantM/alteryx_diff/issues"
   ```
   Note: Confirm the actual GitHub URL from `git remote -v` before writing. If the remote differs, use the actual URL.

4. Do NOT add CONTRIBUTING.md or CODE_OF_CONDUCT.md -- out of scope per user decisions.
5. Do NOT run git filter-branch or BFG -- no secrets were ever committed per research audit.
  </action>
  <verify>
    <automated>cd /Users/laxmikantmukkawar/Documents/Projects/alteryx_diff && test "$(git ls-files .planning/ | wc -l | tr -d ' ')" = "0" && test -f .planning/STATE.md && grep -q 'license' pyproject.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.planning/ has zero files in git index. .planning/ files still exist on local disk. pyproject.toml contains license = MIT and project.urls section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index to public repo | Tracked files become visible when repo goes public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w12-01 | Information Disclosure | .planning/ files in git history | accept | Files contain no secrets (verified by research), only internal planning docs. History scrubbing not needed per user decision. |
| T-w12-02 | Information Disclosure | .env with API key | mitigate | .env was never committed (verified). .gitignore entry prevents future commits. |
| T-w12-03 | Information Disclosure | .claude/ project memory | mitigate | Already untracked. .gitignore entry prevents future commits. |
</threat_model>

<verification>
1. `git ls-files .planning/` returns empty (0 files tracked)
2. `ls .planning/STATE.md` succeeds (files still on disk)
3. `cat LICENSE` shows MIT license with correct copyright
4. `grep -c '.planning/' .gitignore` returns at least 1
5. `grep 'license' pyproject.toml` shows MIT
6. `git status` shows .planning/ files as deleted (staged) and LICENSE, .gitignore, pyproject.toml as modified/new
</verification>

<success_criteria>
- MIT LICENSE file exists at repo root
- .gitignore blocks .env, .env.*, .claude/, .planning/, tests/eval/results/, app/frontend/.vite/
- Zero .planning/ files remain in git index
- All .planning/ files remain on local disk
- pyproject.toml includes license field
- No secrets were ever in git history (confirmed by research, no action needed)
</success_criteria>

<output>
After completion, create `.planning/quick/260414-w12-prepare-the-repo-to-make-it-open-source/260414-w12-SUMMARY.md`
</output>
